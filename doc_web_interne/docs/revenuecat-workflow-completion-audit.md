# RevenueCat Workflow Completion Audit

Date: 2026-05-14

Derniere mise a jour locale: 2026-05-15

Objectif utilisateur: tester et finaliser le workflow RevenueCat avec differents profils utilisateurs et abonnements, du mock valide au Test Store puis Play autant que possible localement.

## Criteres de succes

| Critere | Artefact | Preuve actuelle | Statut |
| --- | --- | --- | --- |
| Visualiser les profils utilisateur subscription | `components/subscription/SubscriptionQALab.tsx` | QA Lab expose guest, new free, existing free, plus user, mode, user, tier, product, renews, quota, auth; les details d'action purchase/restore/refresh/probe affichent aussi `appUserId` quand l'utilisateur est connecte | Fait |
| Tester les profils mock | `maestro/subscription-qa-lab.yml` | Flow couvre guest, new free, plus user, existing free | Fait |
| Tester les etats mock abonnement | `maestro/subscription-qa-lab.yml` | Flow couvre monthly, annual, cancelled, expired et paywall | Fait |
| Valider les packages Test Store sans achat | `maestro/subscription-teststore-readiness.yml` | Relancee le 2026-05-14 sur `Pixel_7_Play_API_36`: mode `RevenueCat Test Store`, `PACKAGES=2`, offres `Buy monthly`/`Buy annual` et prix `$9.99`/`$79.98` visibles | Fait |
| Revalider l'entitlement RevenueCat live | MCP RevenueCat via nouveau `codex exec` | `Noctalia Plus` = `entla0745c9b44`, attache `monthly`, `yearly`, `noctalia_plus:monthly`, `noctalia_plus:annual` | Fait |
| Proteger tout achat Test Store | `scripts/run-subscription-teststore-purchase.js` | Refuse sans `REVENUECAT_QA_APPROVAL=I_APPROVE_TEST_STORE_PURCHASE` | Fait |
| Preflight achat sans Store | `test:e2e:subscription-teststore:purchase:preflight` | Valide plan, credentials et args Maestro sans lancer Maestro ni achat | Fait |
| Preparer le flow achat Test Store | `maestro/subscription-teststore-purchase-manual.yml`, `maestro/subscription-teststore-purchase-google-manual.yml` | Le runner accepte `REVENUECAT_QA_AUTH=email` ou `google`; le flow Google gere `Continue with Google`, le compte deja connecte, la confirmation `TEST VALID PURCHASE`, puis refresh | Fait |
| Capturer les preuves manuelles | `scripts/update-subscription-qa-evidence.js` | Helper remplit `revenuecat-qa-evidence.local.json` avec `testedAt`, `tester`, `appUserId`, `evidence`; `appUserId`, les gates `play_*` `easBuildId`, `versionCode`, les `deviceId` physiques et `installerPackageName=com.android.vending` doivent etre valides; `play_monthly` doit confirmer `P1M`, `play_annual` doit confirmer `P1Y`, et `play_cancellation_and_expiry` doit confirmer annulation/expiration, webhook RevenueCat et convergence backend. Le texte du template, les dates invalides, les identites vides ou en espaces, les ids invalides, les `deviceId` emulateur, les installers non-Play et les `versionCode` manquants/invalides sont refuses | Fait |
| Bloquer release sans preuve complete | `scripts/subscription-qa-report.js --require-full` | `npm run subscription:qa:release-gate` reste rouge sans preuves; une preuve identique au texte du template reste aussi bloquee | Fait |
| Bloquer release Android tant que RevenueCat/Play est rouge | `scripts/check-android-release-gates.js` | `npm run android:gates:strict -- --report-only` relance le 2026-05-15T00:36Z: 10 pass, 1 fail (`RevenueCat subscription QA release gate` avec 1 gate restante `play_cancellation_and_expiry`), 1 blocked (`Play payments profile for Billing` avec 3 exigences ouvertes), 2 manual (`Supabase Play Integrity secrets`, `Play-installed RevenueCat purchase and restore`). Le device ADB est maintenant visible, donc le blocage restant n'est plus le transport ADB. | Fait |
| Revalider le harnais local sans Store | `npm run subscription:qa:verify-local` | Lance les checks syntaxiques, les tests du harnais QA, les tests du gate Android, `subscription:qa:report`, une verification que la release gate bloque sans preuve pour la bonne raison, les preflights d'achat monthly/annual et le preflight account-switch avec comptes factices sans achat ni Metro; le verificateur couvre aussi les snapshots Google OAuth et profil de paiement Play | Fait |
| Extraire l'appUserId RevenueCat du device | `npm run subscription:qa:device-app-user-id -- --device emulator-5554 --env-file .env.teststore` | Ajoute le helper read-only `scripts/extract-revenuecat-app-user-id.js`, qui lit `shared_prefs/com_revenuecat_purchases_preferences.xml` via `adb shell run-as`, masque la cle SDK et retourne l'UUID RevenueCat. Execute le 2026-05-14 sur `Pixel_7_Play_API_36`: `appUserId 1239729f-7468-48c9-b26a-7aa8b4a82591` | Fait local |
| Diagnostiquer les preuves invalides | `scripts/subscription-qa-report.js` | Si un fichier de preuve locale est present, le rapport affiche `Evidence Diagnostics` pour les gates mal remplies | Fait |
| Bloquer un fichier de preuve illisible | `scripts/subscription-qa-report.js` | Un JSON local invalide ne crashe pas le rapport; il bloque sur `Local evidence file parses` | Fait |
| Surfacer les pre-requis de session | `scripts/subscription-qa-report.js` | Le rapport affiche `Current Session Readiness`: variables Test Store presentes/manquantes, approbation d'achat, extraction device de l'appUserId, besoin `ADB: READY`, checks USB/Wireless, Google Play monthly `P1M`, Google Play annual `P1Y`, RevenueCat monthly `LAGGING` et RevenueCat annual `READY` | Fait |
| Verrouiller les signaux readiness/evidence dans le verificateur | `scripts/verify-subscription-qa-local.js` | `npm run subscription:qa:verify-local` echoue si `subscription:qa:report` n'affiche plus `Evidence Commands`, les commandes de preuve `account_switch` / `play_monthly`, `Current Session Readiness`, le compte Test Store env, le second compte account-switch env, l'extraction device appUserId, les rappels `P1M`/`P1Y`, le snapshot profil de paiement Play, ou si les tests du gate Android ne couvrent plus l'etat Wireless Debugging. Les tests bloquent aussi une preuve `play_cancellation_and_expiry` sans annulation/expiration observee, sans webhook ou sans convergence backend. | Fait |
| Diagnostiquer l'appareil Android physique | `npm run android:device:physical`, `npm run android:play-qa-device -- --device 192.168.1.116:41183` | Dernier check de reprise le 2026-05-15: le POCO `192.168.1.116:41183` est visible en ADB Wi-Fi, Play-installed via `com.android.vending`, en `versionCode=24` / `versionName=1.2.0`. Le preflight `--expected-version-code 24` passe; le blocage courant est uniquement `--require-ui-ready` car l'ecran est verrouille (`SCREEN_STATE_OFF` ou `SCREEN_STATE_ON` derriere `NotificationShade`, `uiKeyguardShowing=true`). | Pret, telephone a deverrouiller |
| Valider achat Test Store monthly | `REVENUECAT_QA_AUTH=google ... npm run test:e2e:subscription-teststore:purchase -- --plan monthly --device emulator-5554 --retries 0 --no-restart-metro` | Passe le 2026-05-14 sur `Pixel_7_Play_API_36` avec le compte Google testeur: readiness OK, `TEST VALID PURCHASE`, `monthly purchase completed`, `Refresh completed`, `plus / active` | Fait local Test Store |
| Valider achat Test Store annual | `REVENUECAT_QA_AUTH=google ... npm run test:e2e:subscription-teststore:purchase -- --plan annual --device emulator-5554 --retries 0 --no-restart-metro` | Passe le 2026-05-14 sur `Pixel_7_Play_API_36` avec le compte Google testeur: `annual purchase completed`, `Refresh completed`, `plus / active`; snapshot observe `product yearly`, `renews yes` | Fait local Test Store |
| Valider restore apres achat | `maestro/subscription-teststore-restore-google-manual.yml` + QA Lab Restore | Passe le 2026-05-14 sur `Pixel_7_Play_API_36` apres `adb uninstall com.tanuki75.noctalia` puis reinstall debug Test Store: Google Sign-In refait, `Restore completed`, `Refresh completed`, `plus / active`, snapshot `product yearly`, `renews yes` | Fait local Test Store |
| Reconnecter le compte Google testeur | `Continue with Google` sur `Pixel_7_Play_API_36` | Refait le 2026-05-14 apres signout guard: selection du compte Google testeur dans la feuille native, puis Settings affiche `You're signed in and syncing`, l'email du compte et le QA Lab en mode `RevenueCat Test Store` | Fait local |
| Valider switch compte apres achat | QA Lab + evidence gate `account_switch`; guard local `maestro/subscription-teststore-signout-guard.yml`; flow manuel `maestro/subscription-teststore-account-switch-free-email-manual.yml` | Passe le 2026-05-14 sur `Pixel_7_Play_API_36`: apres achat Test Store monthly du compte principal (`plus / active`), sign-out, creation/confirmation d'un second compte email reel via alias Gmail, login email/password du second compte, puis QA Lab affiche `free (free / inactive)`, `product none`, quota free et `auth ready`. `subscription:qa:evidence` a enregistre `account_switch` dans la preuve locale gitignoree avec l'appUserId RevenueCat `62d96407-ca3c-4493-9c79-5cd8d300d197`. Le flow Maestro a ete durci pour scroller sur `text.subscription.qa.status` et gerer Google Password Manager. | Fait local Test Store |
| Valider Play Internal Testing | Build Play installe, achat Play, backend/webhook | Necessite build installe via Play Internal Testing | Manquant |
| Revalider le dashboard RevenueCat live | MCP RevenueCat OAuth via nouveau `codex exec` le 2026-05-10 | Projet `Noctalia` = `proje6db7596`; offering `default` actif avec packages Test Store et Play | Fait |
| Verifier les builds EAS Android disponibles | Expo MCP `build_list` / `build_info` | Dernier build Store Android `310244ed-027b-4028-8522-70c0f676a0e9`, production AAB, version 1.2.0 build 20, commit SEO du 2026-05-04; ne couvre pas les changements QA actuels | Info, non suffisant |
| Verifier l'inscription Play Testing | Chrome `https://play.google.com/apps/testing/com.tanuki75.noctalia` | Le compte Google `thannous@gmail.com` affiche `You are a tester` pour `Noctalia: Smart Dream Journal` | Fait |
| Verifier Play Console en lecture seule | Chrome Play Console, compte developpeur `TiMax group` (`8905604954685629948`) | Le 2026-05-14, Play Console ouvre `Noctalia: Smart Dream Journal` (`com.tanuki75.noctalia`). `Monetiser avec Play -> Abonnements -> Noctalia Plus` affiche `ID produit noctalia_plus`, deux forfaits de base actifs: `annual` = `Annuel, renouvellement automatique`, actif, et `monthly` = `Mensuel, renouvellement automatique`, actif, retrocompatible; les deux couvrent `174 pays/regions`, avec derniere mise a jour `26 nov. 2025`. La page affiche aussi `Probleme concernant votre profil de paiement`, ce qui reste a resoudre cote Play Console. L'inspection est restee en lecture seule et n'a pas modifie le detail de l'abonnement. | Fait lecture seule |
| Isoler le risque profil de paiement Play | Play Console UI + `npm run android:google-play-payments-profile-state` | Lecture seule le 2026-05-14: la notification critique indique `Un probleme urgent requiert votre attention concernant votre compte de paiement`. `Profil de paiement` demande encore les informations fiscales (`Mettre a jour`), un mode de paiement valide (`Mettre a jour`) et les informations fiscales pour l'Irlande (`Ajouter des infos fiscales`). Snapshot local gitignore `doc_web_interne/docs/google-play-payments-profile-state.local.json` cree; `android:gates:strict` bloque maintenant ce point avec 3 exigences ouvertes jusqu'a resolution dans Play Console. | Bloque Play Console |
| Rendre les secrets Supabase Play Integrity auditables | `npx supabase secrets list --project-ref usuyppgsmmowzizhaoqj`, `scripts/update-supabase-play-integrity-secrets-state.js` | Relance le 2026-05-14T22:45Z puis `npx supabase projects list` le 2026-05-15: le CLI Supabase repond `Access token not provided`, et aucun env `SUPABASE_*` n'est present dans la session. Le gate reste donc `CHECK LIVE` dans `subscription:qa:report`. Un snapshot local gitignore `doc_web_interne/docs/supabase-play-integrity-secrets-state.local.json` peut enregistrer uniquement la presence de `PLAY_INTEGRITY_SERVICE_ACCOUNT_JSON_BASE64`, `PLAY_INTEGRITY_PACKAGE_NAME=com.tanuki75.noctalia`, et `GUEST_SESSION_SECRET`; les valeurs secretes ne sont pas stockees. | Pret, login Supabase requis |
| Verifier Play Developer API en lecture seule | `gcloud auth application-default login --scopes=androidpublisher,cloud-platform`, quota project `gen-lang-client-0336445544`, endpoint officiel `GET /androidpublisher/v3/applications/com.tanuki75.noctalia/subscriptions/noctalia_plus` | Le 2026-05-14, l'API Google Play confirme directement `productId=noctalia_plus`: base plan `monthly` actif en `P1M`, disponible nouveaux abonnes US/FR, prix US `$3.49`, FR `EUR 3.59`; base plan `annual` actif en `P1Y`, prix US `$21.99`, FR `EUR 22.99`. Le tag Play du monthly est `monthy` (typo existante), mais le `basePlanId` et la periode API sont corrects. | Fait lecture seule |
| Preparer un emulateur Google Play | Android SDK + AVD `Pixel_8_Play_API_36` | Image `system-images;android-36;google_apis_playstore;arm64-v8a` installee, AVD cree, `adb shell pm list packages com.android.vending` confirme `package:com.android.vending` | Fait |
| Connecter le compte Play dans l'emulateur | Google sign-in sur `Pixel_8_Play_API_36` | `adb shell dumpsys account` confirme `Account {name=thannous@gmail.com, type=com.google}` | Fait |
| Installer Noctalia depuis Play Internal Testing | Google Play sur `Pixel_8_Play_API_36` | La fiche Play affiche `Noctalia: Smart Dream Journal (Beta)`, `You're a beta tester for this app`, mais bloque l'installation avec `Your device isn't compatible with this version` | Bloque Play/device catalog |
| Verifier la compatibilite technique du bundle avec l'emulateur | `bundletool get-device-spec` + `bundletool build-apks` | L'AAB `cbbf745a-0e76-4488-a365-ba180a903e90` genere `/private/tmp/noctalia-cbbf745a.apks` pour `emulator-5554`: `base-arm64_v8a.apk`, `base-en.apk`, `base-master.apk`, `base-xxhdpi.apk`; aucun `uses-feature`, `supports-screens` ou `compatible-screens` bloquant n'a ete trouve dans le manifest dump | Fait |
| Verifier la compatibilite technique du bundle avec le POCO | `bundletool get-device-spec --device-id=192.168.1.116:41183`, puis `bundletool build-apks --bundle=/private/tmp/noctalia-v24.aab --device-spec=/private/tmp/noctalia-poco-25102pcbeg-device-spec.json` | Le 2026-05-15, le POCO `25102PCBEG` expose `arm64-v8a`, SDK 36, densite 480, locale `fr-FR`; bundletool genere `/private/tmp/noctalia-v24-poco-25102pcbeg.apks` (66M) avec `base-arm64_v8a.apk`, `base-fr.apk`, `base-master.apk`, `base-xxhdpi.apk`. Cette preuve confirme que le build 24 est techniquement splittable pour le POCO; le fait que Play ne propose pas `Mettre a jour` reste donc cote disponibilite/update/cache Play, pas un filtre bundletool evident. | Fait lecture seule |
| Reduire les filtres hardware Play inutiles | `plugins/withOptionalAndroidHardwareFeatures.js`, `app.json` | Le config plugin declare `android.hardware.camera`, `android.hardware.camera.any` et `android.hardware.microphone` avec `android:required="false"` dans le manifest genere; verification locale via `@expo/config-plugins` OK | Corrige localement, nouveau build requis |
| Rebuild Store apres correctif hardware | EAS CLI depuis worktree propre `/private/tmp/noctalia-eas-c19bf249` | Build Android Store `ddbc756d-8db6-4337-80fa-68cc86f8b62a` fini le 2026-05-11, versionCode 24, commit `c19bf249141f443b84d232bb1c5d9708cbb5caae`, AAB `https://expo.dev/artifacts/eas/to6rt1WLAABVH7Ca3o4mBm.aab`; soumission Internal Testing `99d2a4b1-3eac-4479-850f-bf179c13af91` = `FINISHED`, track `internal`, release `COMPLETED`, sans erreur | Fait |
| Verifier la piste Internal Testing live | Google Play Developer API `edits.tracks.get` sur `track=internal`, edit temporaire supprime sans commit | Le 2026-05-15, l'API Play retourne `track=internal`, release `1.2.0`, `versionCodes=["24"]`, `status=completed`. Cela confirme que le build 24 est bien servi par la piste internal cote Play; apres acceptation de l'opt-in internal, le POCO a finalement ete mis a jour depuis Play vers ce `versionCode=24`. | Fait lecture seule |
| Comparer toutes les pistes Play live | Google Play Developer API `edits.tracks.list`, edit temporaire supprime sans commit | Le 2026-05-15, l'API Play liste `production` en `1.1.0/versionCode=12`, `beta` en `11`, `alpha` en draft `1`, et `internal` seul en `1.2.0/versionCode=24`. Ce snapshot a explique pourquoi le POCO servait d'abord `12`; l'opt-in internal puis l'update Play ont ensuite permis d'installer `24`. | Fait lecture seule |
| Verifier l'opt-in internal du compte testeur | Play Console `Tests internes -> Testeurs`, puis page `https://play.google.com/apps/internaltest/4700165416980291595` | Le 2026-05-15: la piste internal est active, la derniere release est `1.2.0`, les listes `family` et `Testeurs de licence (generation automatique)` sont cochees, et la liste generee contient `thannous@gmail.com`. La page d'opt-in internal affichait `Accept invite`; l'invitation a ete acceptee et affiche maintenant `You are a tester`. | Fait |
| Verifier les APKs generes par Play | Google Play Developer API `generatedApks.list` sur `com.tanuki75.noctalia`, versionCode `24`; `aapt dump badging`; `apksigner verify --print-certs` | Le 2026-05-14, l'API Play renvoie un `generatedUniversalApk` et des splits pour le build 24 sous le certificat Play App Signing `6A:8C:B2:E2:CD:D2:C1:FD:D7:C5:CF:CF:00:D7:C3:CC:58:61:C2:CD:3F:AF:49:F6:9A:31:25:35:A4:4F:EE:0F`. L'APK universel telecharge en `alt=media` pese 131M; `aapt` confirme `package=com.tanuki75.noctalia`, `versionCode=24`, `versionName=1.2.0`, `minSdk=33`, `targetSdk=36`, permission `com.android.vending.BILLING`; `apksigner` confirme le SHA-256 Play App Signing `6a8cb2e2cdd2c1fdd7c5cfcf00d7c3cc5861c2cd3faf49f69a312535a44fee0f`. Cela confirme que Play a genere et signe un APK coherent; l'incompatibilite vue sur les AVD reste donc probablement cote device catalog / track availability plutot qu'un echec de generation APK. | Fait lecture seule |
| Extraire le SHA-1 Play App Signing | `/Users/tanuki/Library/Android/sdk/build-tools/36.0.0/apksigner verify --print-certs /private/tmp/noctalia-play-universal-v24.apk` | Le certificat Play App Signing extrait de l'APK universel Play versionCode 24 donne `SHA-1 = BC:CF:C2:96:38:47:81:D6:8C:B7:B6:5A:BA:84:CB:B3:8C:85:E0:59` et `SHA-256 = 6A:8C:B2:E2:CD:D2:C1:FD:D7:C5:CF:CF:00:D7:C3:CC:58:61:C2:CD:3F:AF:49:F6:9A:31:25:35:A4:4F:EE:0F`. Cette preuve fournit la valeur a ajouter/verifier dans le client OAuth Android Google Cloud; elle ne prouve pas encore que Google Cloud est configure. | Fait local, OAuth Google Cloud a confirmer |
| Revalider Play apres build 24 | Google Play sur `Pixel_8_Play_API_36` + `adb exec-out uiautomator dump /dev/tty` | La fiche Play affiche toujours `Your device isn't compatible with this version` et `You're a beta tester for this app`; recheck de propagation relance le 2026-05-11 avec le meme resultat; Noctalia n'est pas installee | Bloque Play/device catalog |
| Verifier les device tiers Play via API | Google Play Developer API `applications.deviceTierConfigs.list` avec quota project `gen-lang-client-0336445544` | Le 2026-05-14, l'endpoint officiel renvoie `204 No Content`: aucune configuration de device tiers personnalisee n'est exposee par l'API pour `com.tanuki75.noctalia`. Cela ne prouve pas l'absence de tout filtre Play Console, mais ecarte une device tier config API comme cause directe du blocage AVD. | Fait lecture seule |
| Verifier les filtres manifest de l'APK Play | `aapt dump badging`, `aapt dump xmltree AndroidManifest.xml`, `adb getprop/wm` sur `emulator-5554` | L'APK universel Play `versionCode=24` declare `sdkVersion=33`, `targetSdkVersion=36`, `supports-screens small/normal/large/xlarge`, densites `120/160/240/320/480/640/65534/65535`, native-code `arm64-v8a/armeabi-v7a/x86/x86_64`, et `com.android.vending.BILLING`. Les seules `uses-feature` hardware vues sont `android.hardware.camera`, `android.hardware.camera.any` et `android.hardware.microphone`, toutes en `required=false`. L'emulateur courant est API 36, arm64-v8a, 1080x2400, densite 420. Aucun filtre manifest evident n'explique donc l'incompatibilite Play AVD. | Fait lecture seule |
| Verifier le lancement local du build 24 hors Play | `bundletool build-apks` signe avec `/private/tmp/noctalia-debug.keystore`, puis `bundletool install-apks --device-id=emulator-5554` | Installation locale OK; `dumpsys package` affiche `versionCode=24`, `versionName=1.2.0`, `installerPackageName=null`; `adb shell am start -n com.tanuki75.noctalia/.MainActivity` affiche l'ecran d'accueil Noctalia. Cette preuve isole le binaire comme installable/lancable, mais ne valide pas Play Billing car l'app n'est pas installee par `com.android.vending` | Info, non suffisant |
| Verifier le sideload direct de l'APK universel Play | `adb install /private/tmp/noctalia-play-universal-v24.apk`; `adb shell am start -n com.tanuki75.noctalia/.MainActivity`; `dumpsys package` | Le 2026-05-14, l'APK universel signe par Play s'installe apres desinstallation du build local incompatible (`versionCode=24`, `versionName=1.2.0`, `installerPackageName=null`, `apkSigningVersion=3`). Au lancement, `MainActivity` demarre puis bascule vers `com.pairip.licensecheck.LicenseActivity`, qui affiche Play Store `Get this app from Play`. Le sideload Play-signe ne permet donc pas de tester le parcours RevenueCat/Play localement; il faut une vraie installation via Play Internal Testing. | Bloque, non substitut Play |
| Verifier l'etat store live des produits Play | RevenueCat MCP `list_offerings expand=items.package.product`, `get_product_store_state` | Relu le 2026-05-14T15:36Z via un `codex exec` frais: `prodfce10ef2a8` repond toujours `annual/P1Y/ACTIVE` dans `store_state.base_plans`, et `prod98337b31be` repond `annual/P1Y/ACTIVE`. Le catalogue RevenueCat reste coherent par ailleurs (`$rc_monthly` -> `prodfce10ef2a8`, `store_identifier=noctalia_plus:monthly`, `subscription.duration=P1M` dans les metadonnees produit), mais le store-state mensuel continue de lagger derriere Google Play direct. | Partiel, store_state RevenueCat lag |
| Enregistrer le snapshot Play Store state | `doc_web_interne/docs/revenuecat-play-store-state.local.json` | Snapshot local gitignore rafraichi le 2026-05-14T15:37:16Z depuis le resume MCP frais. `subscription:qa:report` le lit et affiche `Play monthly base plan snapshot` comme `LAGGING` quand Google Play direct est `monthly/P1M/ACTIVE` mais que RevenueCat store-state lit encore `prodfce10ef2a8` en `annual/P1Y`; il affiche aussi `Play annual base plan snapshot` comme `READY` avec `prod98337b31be: annual/P1Y`. Sans snapshot Google Play direct pret, la contradiction mensuelle bloque `play_monthly`. | Fait local, RevenueCat lag |
| Mettre a jour le snapshot Play Store state sans collage fragile | `npm run subscription:qa:play-state -- --input revenuecat-store-state.json` | Helper ajoute le 2026-05-14: il accepte le JSON MCP compact en fichier ou stdin, exige les produits RevenueCat `prodfce10ef2a8` et `prod98337b31be`, normalise les base plans dans `revenuecat-play-store-state.local.json`, puis demande de relancer `subscription:qa:report` pour classer un store-state non-P1M en `BLOCKED` ou `LAGGING` selon le snapshot Google Play direct. | Fait |
| Verifier si Google Play Developer API est utilisable localement | `gcloud auth application-default login --scopes=https://www.googleapis.com/auth/androidpublisher,https://www.googleapis.com/auth/cloud-platform`, quota project `gen-lang-client-0336445544`, endpoint `subscriptions.get` | Debloque le 2026-05-14: l'ADC locale avec scope Android Publisher et quota project lit `noctalia_plus` en lecture seule. L'endpoint incorrect `/monetization/subscriptions/...` renvoie 404; l'endpoint officiel `/applications/{packageName}/subscriptions/{productId}` renvoie les base plans actifs. | Fait lecture seule |
| Enregistrer le snapshot Google Play API | `doc_web_interne/docs/google-play-subscription-state.local.json`, `scripts/update-google-play-subscription-state.js` | Apres reauth ADC Android Publisher et ajout du quota project, le snapshot local gitignore parse la reponse officielle `subscriptions.get` et `subscription:qa:report` affiche `Google Play monthly base plan snapshot` comme `READY` avec `monthly/P1M/ACTIVE` et `Google Play annual base plan snapshot` comme `READY` avec `annual/P1Y/ACTIVE`; snapshot rafraichi le 2026-05-14T15:21:48Z, et le snapshot RevenueCat mensuel est maintenant signale comme `LAGGING` tant que `prodfce10ef2a8` lit encore `annual/P1Y`. | Fait local |
| Eviter un mauvais libelle si le Store contredit le package | `lib/revenuecat.ts`, `lib/__tests__/revenuecat.test.ts` | Le mapping utilise maintenant `product.subscriptionPeriod` (`P1M`/`P1Y`) avant `packageType`/id; si le produit `monthly` revient en `P1Y`, l'app ne le presente plus comme mensuel | Corrige localement, config live encore a corriger |
| Verifier les preuves locales manuelles | `doc_web_interne/docs/revenuecat-qa-evidence.local.json` | Fichier local gitignore mis a jour avec `test_store_monthly`, `test_store_annual`, `restore_after_reinstall`, `account_switch`, `play_monthly` et `play_annual` passes. `subscription:qa:report` affiche maintenant 6 scenarios manuels/externes verifies et 1 gate restante (`play_cancellation_and_expiry`). | Partiel local |
| Verifier l'installation Play avant preuve Play | `npm run android:play-install-source -- --device <adb-id>` | Helper ajoute le 2026-05-14: lit `adb shell dumpsys package com.tanuki75.noctalia`, affiche version/installer, et echoue tant que `installerPackageName` n'est pas `com.android.vending`. Cela bloque explicitement les preuves issues d'un sideload, d'un build debug ou d'un APK Play-signe installe via ADB. | Pret, appareil Play requis |
| Distinguer emulateur et appareil Play reel | `npm run android:device:physical` | Ajoute le 2026-05-14: reutilise le diagnostic ADB mais echoue si seuls des AVD `emulator-*` ou `sdk_gphone*` sont visibles. Les tests mock/Test Store peuvent continuer sur emulateur, mais les gates `play_*` exigent un telephone Android reel avant la preuve `installerPackageName=com.android.vending`. | Pret, appareil physique requis |
| Preflight device Play complet | `npm run android:play-qa-device -- --device <adb-id>` | Ajoute le 2026-05-14 et durci le 2026-05-15: compose `android:device:physical`, `android:play-install-source` et une lecture `dumpsys window policy`; auto-selectionne l'unique telephone physique ou demande `--device <adb-id>` si plusieurs devices sont prets, puis echoue si l'app n'est pas installee par Google Play. Quand il passe, il imprime `evidenceArgs` avec `--device-id <adb-id> --installer-package-name com.android.vending --version-code <n>` et les trois commandes d'evidence `play_monthly`, `play_annual` et `play_cancellation_and_expiry`; il affiche aussi `ui: READY` ou `ui: WARN` pour signaler si le telephone doit etre deverrouille avant les flows UI. | Pret, appareil physique requis |
| Attendre le device Play sans relancer a la main | `npm run android:play-qa-device:wait` | Ajoute le 2026-05-14 et etendu le 2026-05-15: poll le preflight Play QA jusqu'a ce qu'un telephone physique soit visible et que Noctalia soit installee via Google Play; quand il passe, il imprime les commandes d'evidence `play_*`. Utiliser `-- --device <adb-id>` seulement quand plusieurs devices sont prets. Options: `--interval-ms`, `--timeout-ms`, `--expected-version-code <n>` pour attendre une build Play precise, et `--require-ui-ready` pour attendre aussi un telephone eveille/deverrouille avant les flows d'achat ou restore pilotes par UI. | Pret, appareil physique requis |
| Preflight feuille Play Billing mensuelle | POCO `192.168.1.116:41183`, app Play-installed, paywall mensuel | Le 2026-05-15, la feuille Google Play s'ouvre pour `$rc_monthly` avec `Noctalia Plus`, `3,59 EUR/5 min`, `Carte test, toujours approuvee`, et le message `Il s'agit d'un abonnement test ... Il ne vous sera pas facture`. La feuille est fermee sans appuyer sur `S'abonner`; les logs RevenueCat affichent `PurchaseCancelledError` et `packageId: '$rc_monthly'`. Cela confirme que Play Billing est joignable et non facture, mais ne ferme pas `play_monthly`. | Pret, achat non valide |
| Valider achat Play monthly | POCO `192.168.1.116:41183`, Google Play Internal Testing `versionCode=24`, Google Play Billing + Google Play Developer API `purchases.subscriptionsv2.get` | Le 2026-05-15, achat test mensuel valide apres confirmation utilisateur: feuille `3,59 EUR/5 min`, `Carte test, toujours approuvee`, non-facturation; logs app `Purchase completed`, `packageId: '$rc_monthly'`, `newTier: 'plus'`, `isActive: true`; UI `Noctalia Plus est actif`, valide jusqu'a `01:56`; restore RevenueCat ensuite `restoredTier: 'plus'`; API Play retourne `SUBSCRIPTION_STATE_ACTIVE`, `ACKNOWLEDGEMENT_STATE_ACKNOWLEDGED`, `testPurchase=true`, `basePlanId=monthly`. Evidence locale `play_monthly` enregistree. | Fait Play test |
| Observer cancellation/expiry Play monthly | Google Play Developer API cancel + expiry test 5 min + restore app | Le 2026-05-15, annulation API mensuelle retourne HTTP `204`, puis `purchases.subscriptionsv2.get` passe a `SUBSCRIPTION_STATE_CANCELED` avec `developerInitiatedCancellation`. Apres `expiryTime=2026-05-14T23:56:19.254Z`, l'API Play retourne `SUBSCRIPTION_STATE_EXPIRED`; restore app retourne `restoredTier: 'free'`, `isActive: false`, et l'UI repasse a `Augmenter mes limites`. La gate `play_cancellation_and_expiry` reste non enregistree car RevenueCat MCP/backend webhook n'est pas relisible dans cette session (`403 token revoked`). | Partiel, webhook/backend a relire |
| Valider achat Play annual | POCO `192.168.1.116:41183`, Google Play Internal Testing `versionCode=24`, Google Play Billing + Google Play Developer API `purchases.subscriptionsv2.get` | Le 2026-05-15, achat test annuel valide apres retour free: feuille `22,99 EUR/30 min`, `Carte test, toujours approuvee`, non-facturation; logs app `Purchase completed`, `packageId: '$rc_annual'`, `newTier: 'plus'`, `isActive: true`; UI `Noctalia Plus est actif`, valide jusqu'a `02:28`; restore RevenueCat ensuite `restoredTier: 'plus'`; API Play retourne `SUBSCRIPTION_STATE_ACTIVE`, `ACKNOWLEDGEMENT_STATE_ACKNOWLEDGED`, `testPurchase=true`, `basePlanId=annual`. Evidence locale `play_annual` enregistree; l'abonnement annuel test a ensuite ete annule par API (HTTP `204`) et Play retourne `SUBSCRIPTION_STATE_CANCELED` jusqu'a son expiry test. | Fait Play test, annual annule |
| Lier la preuve Play au device teste | `npm run subscription:qa:evidence -- --gate play_* --device-id <adb-id> --installer-package-name com.android.vending --version-code <n>` | Ajoute le 2026-05-14 et durci le 2026-05-14T22:51Z: les gates `play_*` exigent maintenant `deviceId`, `installerPackageName` et `versionCode` dans la preuve locale, refusent les ids `emulator-*`, les installers non-Play et les `versionCode` manquants ou invalides. Le rapport QA signale `deviceId is required for Play evidence`, `installerPackageName is required for Play evidence` ou `versionCode is required for Play evidence` si une preuve Play reste trop vague. | Pret, appareil physique requis |

## Prompt-to-artifact checklist

| Exigence explicite | Fichier / commande | Verification |
| --- | --- | --- |
| Differents profils utilisateur | `maestro/subscription-qa-lab.yml` | `npm run test:e2e:subscription-qa -- --device emulator-5554 --retries 0` a passe apres durcissement du flow et reload Metro |
| Differents abonnements | `services/mocks/subscriptionServiceMock.ts`, `SubscriptionQALab.tsx`, `subscription-qa-lab.yml` | Mock monthly/annual/cancelled/expired couverts |
| Test Store | `.env.teststore`, `eas.json`, `subscription-teststore-readiness.yml` | `node ./scripts/run-maestro-android.js --suite store --env-file .env.teststore --device emulator-5554 --retries 0 --no-restart-metro` passe le 2026-05-14 avec packages 2 et les deux offres visibles |
| Google Play | `.env.playstore`, `eas.json`, `constants/subscription.ts` | Config presente; `subscription:qa:report` verifie `release`, `preview`, `production`, `production-apk`; achat Play non execute |
| Entitlement live | MCP RevenueCat `list_entitlements` via nouveau `codex exec` | `Noctalia Plus` existe et attache les produits Test Store `monthly`/`yearly` et Play `noctalia_plus:monthly`/`noctalia_plus:annual` |
| Visualiser les workflows | `SubscriptionQALab.tsx` | Surface Settings affiche mode, SDK key, packages, user, tier, product, renews, quota, auth, actions; les details d'action incluent `appUserId` quand disponible pour faciliter la preuve structuree |
| Etre sur que tout fonctionne | `subscription:qa:release-gate` | Gate bloque tant que les preuves manuelles/externes sont absentes |
| Ne pas publier Android si RevenueCat/Play n'est pas robuste | `npm run android:gates:strict` | Gate strict Android echoue tant que `subscription:qa:release-gate` echoue; le rapport affiche `RevenueCat subscription QA release gate` comme `FAIL`, bloque `Play payments profile for Billing` si le snapshot contient des exigences ouvertes, peut passer ou bloquer `Supabase Play Integrity secrets` depuis un snapshot local sans valeurs secretes, remonte l'etat USB et ADB Wireless quand aucun device n'est visible, et conseille le wait helper Play QA avant les preuves Play-installed |
| Ne pas confondre mock et reel | `subscription-qa-report.js`, `revenuecat-qa-workflow.md` | Matrice separe Automated, Manual purchase gate, External store gate |
| Enregistrer les preuves | `revenuecat-qa-evidence.example.json`, `update-subscription-qa-evidence.js`, `extract-revenuecat-app-user-id.js` | Tests prouvent evidence -> report -> gate count; les gates Play sans `easBuildId` UUID, sans `versionCode` entier positif, sans `deviceId` physique, sans `installerPackageName=com.android.vending` ou sans confirmation texte `com.android.vending` restent bloquees; `play_monthly` exige `P1M`, `play_annual` exige `P1Y`, et `play_cancellation_and_expiry` exige annulation/expiration observee, webhook RevenueCat et convergence backend. Les preuves sans `appUserId` UUID, les textes de preuve restes au template, les dates invalides, les identites en espaces et les JSON invalides ne comptent pas. Le 2026-05-14, l'appUserId RevenueCat a ete extrait des preferences app debug via le helper device et les preuves locales Test Store monthly, annual, restore apres reinstall et account-switch ont ete inscrites dans le fichier gitignore |
| Revalidation live RevenueCat | MCP RevenueCat `list_projects` puis offerings/packages via nouveau `codex exec` | `Noctalia` = `proje6db7596`; offering `default` actif; `$rc_monthly` -> `monthly` + `noctalia_plus:monthly`; `$rc_annual` -> `yearly` + `noctalia_plus:annual` |
| Preflight achat | `npm run test:e2e:subscription-teststore:purchase:preflight -- --plan monthly --device emulator-5554` | Ne necessite pas `REVENUECAT_QA_APPROVAL`, ne lance pas Maestro, valide les prerequisites locaux |
| Verification locale rapide | `npm run subscription:qa:verify-local` | Regroupe syntax checks, tests scripts QA et gate Android, rapport de couverture avec `Evidence Commands` et `Current Session Readiness`, blocage attendu de la release gate sans preuve pour la bonne raison, preflights d'achat monthly/annual, preflight account-switch sans ouvrir le Store, helper device appUserId, readiness monthly/annual Play, Google OAuth, profil de paiement Play et snapshot secrets Supabase Play Integrity |
| Readiness de la session courante | `npm run subscription:qa:report` | Section `Current Session Readiness` affiche actuellement `REVENUECAT_QA_EMAIL=missing`, `REVENUECAT_QA_PASSWORD=missing`, approbation Test Store `NOT SET`, extraction device appUserId a lancer avant preuve manuelle, besoin `ADB: READY`, Google Play monthly `READY` en `monthly/P1M/ACTIVE`, Google Play annual `READY` en `annual/P1Y/ACTIVE`, RevenueCat monthly `LAGGING` en `prodfce10ef2a8: annual/P1Y`, et RevenueCat annual `READY` en `prod98337b31be: annual/P1Y` |
| Snapshot RevenueCat/Play live | `doc_web_interne/docs/revenuecat-play-store-state.local.json`, `doc_web_interne/docs/revenuecat-play-store-state.example.json` | Le fichier local est gitignore; le rapport le parse quand il existe et refuse de laisser passer `play_monthly` si le produit `prodfce10ef2a8` ne contient pas un billing period `P1M`, sauf quand le snapshot Google Play direct est `monthly/P1M/ACTIVE` et qu'une vraie preuve Play-installed/backend convergee est fournie. Il verifie aussi que `play_annual` expose `P1Y` via `prod98337b31be`. |
| Snapshot Google Play API direct | `doc_web_interne/docs/google-play-subscription-state.local.json`, `doc_web_interne/docs/google-play-subscription-state.example.json` | Le fichier local est gitignore; le rapport le parse quand il existe et separe la preuve Play Console/API (`monthly/P1M/ACTIVE`, `annual/P1Y/ACTIVE`) du blocage RevenueCat (`prodfce10ef2a8: annual/P1Y`) |
| Mise a jour reproductible du snapshot Play | `scripts/update-revenuecat-play-store-state.js`, `scripts/update-revenuecat-play-store-state.test.js`, `subscription:qa:play-state` | Tests couvrent le JSON MCP compact, le snapshot non-P1M `annual/P1Y`, le snapshot pret `monthly/P1M`, les produits manquants, les dates invalides, et le message de suivi qui renvoie vers `subscription:qa:report` pour classer `BLOCKED` vs `LAGGING` |
| API Google Play Developer directe | ADC/gcloud local + endpoint subscriptions get | Reauth ADC avec scope `androidpublisher` puis quota project `gen-lang-client-0336445544` reussis le 2026-05-14; l'endpoint officiel confirme `monthly/P1M/ACTIVE` et `annual/P1Y/ACTIVE` pour `noctalia_plus` |
| Verifier le projet Google Cloud OAuth localement | `gcloud config list`, `gcloud auth list`, `gcloud projects list --filter=PROJECT_NUMBER=359653779023`, `gcloud iam oauth-clients list --project gen-lang-client-0336445544 --location=global` | Le 2026-05-14, `gcloud` est authentifie avec `thannous@gmail.com`, le project number `359653779023` correspond a `gen-lang-client-0336445544` / `dreamweaver`, et `androidpublisher.googleapis.com` est active. L'API officielle `gcloud iam oauth-clients list` existe mais renvoie `[]` pour ce projet; elle ne prouve donc pas la presence du client Android classique `359653779023-b2ehl3qp5eas6b8ncu4cenjbtb3ivqfp.apps.googleusercontent.com`. Verification Google Cloud Console encore requise pour confirmer que le SHA-1 Play `BC:CF:C2:96:38:47:81:D6:8C:B7:B6:5A:BA:84:CB:B3:8C:85:E0:59` est bien attache au client Android. |
| Verifier le client OAuth Android Play | Google Cloud Console `Google Auth Platform -> Clients -> Noctalia Android Production`; `npm run android:google-oauth-android-client-state` | Lecture seule le 2026-05-14: le client Android `Noctalia Android Production` (`359653779023-5dhs012rh7l3cjf0leoknn7j0dlgq0ok.apps.googleusercontent.com`) declare `package_name=com.tanuki75.noctalia` et le SHA-1 Play App Signing `BC:CF:C2:96:38:47:81:D6:8C:B7:B6:5A:BA:84:CB:B3:8C:85:E0:59`. Snapshot local gitignore `doc_web_interne/docs/google-oauth-android-client-state.local.json` mis a jour; `android:gates:strict` peut maintenant passer ce point si le snapshot reste coherent. | Fait lecture seule |
| Appareil physique Android | `npm run android:device:physical` | Dernier check le 2026-05-14T21:40Z: le binaire `adb` est installe (`/Users/tanuki/Library/Android/sdk/platform-tools/adb`, platform-tools 37.0.0), `adb devices -l` ne voit aucun device, mais macOS USB voit `POCO F8 Ultra, Xiaomi, serial 57275d36`, `idVendor 10007`, `idProduct 65352`, avec `Android debug interface signature present`; `WIRELESS: NOT VISIBLE`. La sortie humaine du diagnostic distingue maintenant `ADB` (outil adb) de `ADB DEVICE` (appareil visible par adb). Le blocage restant est donc l'autorisation/transport ADB cote telephone, pas le cable ni l'installation adb. |
| Build EAS Android existant | Expo MCP `build_info 310244ed-027b-4028-8522-70c0f676a0e9` | Build fini, STORE, production, AAB disponible, mais date/commit avant le workflow QA RevenueCat courant |
| Build EAS Android utilisable pour Play | EAS CLI depuis worktree propre + Expo MCP `build_info cbbf745a-0e76-4488-a365-ba180a903e90` + page Expo Submission Details | Build Store Android `cbbf745a-0e76-4488-a365-ba180a903e90` fini le 2026-05-10, versionCode 22, commit `723e6deb0524e3b49e33045d2298da918404e40f`, AAB `https://expo.dev/artifacts/eas/rag5GTaMLdJ4HAYpS4vqDo.aab`; auto-submit Internal Testing `6d4630b0-33e1-48d7-a8f1-687206b2fe8f` confirme `Success` sur Expo, soumis le 2026-05-10 a 23:29 | Fait |
| Inscription Play Testing | Chrome avec `thannous@gmail.com` sur `play.google.com/apps/testing/com.tanuki75.noctalia/join?hl=en-US` | Page affiche `You are a tester`; la fiche Play sur l'emulateur affiche aussi Noctalia Beta et `You're a beta tester for this app` | Fait |
| Emulateur Google Play local | `sdkmanager`, `avdmanager`, `adb`, `bundletool` | `Pixel_8_Play_API_36` utilise `google_apis_playstore`, boote sur `emulator-5554`, expose `com.android.vending`, contient le compte Google `thannous@gmail.com`; Play bloque l'installation par compatibilite alors que bundletool genere des APKs adaptes au device spec | Bloque Play/device catalog |
| Emulateur Play alternatif | `Pixel_7_Play_API_36`, `adb`, Play Store | AVD cree le 2026-05-14 avec la meme image `google_apis_playstore`; il boote, expose `com.android.vending`, compte `thannous@gmail.com` connecte apres 2FA POCO. Recheck local le 2026-05-14: la fiche Play affiche `Noctalia: Smart Dream Journal (Beta)`, `You're a beta tester for this app`, puis `This app is available only for your other devices`; elle liste aussi `POCO 25102PCBEG` comme `Installed` et `HONOR PGT-N19` comme installable. Le Pixel 7 emulateur est donc toujours filtre par le catalogue Play, tandis que le parcours Play reel doit etre repris sur un telephone physique visible en ADB. | Bloque Play/device catalog |
| Emulateur Play API 35 | `sdkmanager`, `avdmanager`, `Pixel_6_Play_API_35`, Google sign-in | Image `system-images;android-35;google_apis_playstore;arm64-v8a` installee le 2026-05-14, AVD `Pixel_6_Play_API_35` cree et boote en Android 15 avec `com.android.vending`. La relance du 2026-05-14T20:42Z a valide le login Google apres MFA telephone (`Check your phone`, code `62`), `adb shell dumpsys account` confirme `Account {name=thannous@gmail.com, type=com.google}`, puis la fiche Play Noctalia beta affiche `Your device isn't compatible with this version`. L'API 35 ne contourne donc pas le filtrage catalogue Play AVD. | Bloque Play/device catalog |
| Device Catalog emulateur Play | Play Console `Surveiller et ameliorer -> Portee et appareils -> Catalogue d'appareils` | Inspection lecture seule le 2026-05-14: en `Tous les appareils`, la recherche `emu64a` retourne `0 modele d'appareil`; en `Appareils compatibles`, `sdk_gphone`/`emu64xa` retourne uniquement `google emu64xa`, nom commercial `Google emulator`, Android `16 Beta`, SoC `Intel 3215U`, etat `Compatible`; en `Appareils exclus`, `emu64a` retourne aussi `0 modele exclus`. Nos AVD Apple Silicon exposent `sdk_gphone64_arm64` / `device:emu64a`, donc ils ne correspondent pas au modele emulateur compatible visible par Play. Cause la plus probable du message Play: le modele AVD arm64 Apple Silicon n'est pas targetable dans le catalogue Play de l'app, meme si le manifest et `bundletool` acceptent le binaire. | Bloque Play/device catalog |
| Filtrage hardware Play | `withOptionalAndroidHardwareFeatures`, AAB `ddbc756d-8db6-4337-80fa-68cc86f8b62a` | Manifest du build 24 contient `uses-feature` camera, camera.any et microphone en `android:required="false"`; `bundletool build-apks` genere encore les splits `base-arm64_v8a`, `base-en`, `base-master`, `base-xxhdpi` pour `emulator-5554`; Play Store continue de filtrer l'AVD | Corrige, blocage Play persistant |
| Lancement hors Play du build 24 | `bundletool install-apks` + `adb shell am start` | Les splits signes localement s'installent sur `Pixel_8_Play_API_36` et l'app affiche l'ecran de capture de reve; `installerPackageName=null`, donc cette preuve ne ferme aucune gate Play | Info, non suffisant |
| Produit Play mensuel live cote RevenueCat | RevenueCat MCP `list_products`, `list_packages`, puis `get_product_store_state(project_id=proje6db7596, product_id=prodfce10ef2a8)` | Relu le 2026-05-14: le produit RevenueCat mensuel a bien `store_identifier=noctalia_plus:monthly`, `subscription.duration=P1M`, `display_name=Noctalia Plus Monthly (Play Store)` et il est attache au package `$rc_monthly`. La sous-partie `store_state.base_plans` de ce meme produit remonte pourtant `annual/P1Y`; l'API Google Play directe confirme `monthly/P1M/ACTIVE`. Le rapport QA accepte donc une future preuve d'achat Play mensuel si elle converge reellement et si le snapshot Google Play direct reste `P1M`, mais garde le snapshot RevenueCat en alerte rouge. | Partiel, achat Play requis |
| Mapping intervalle SDK | `mapIntervalFromId(..., subscriptionPeriod)` | `P1Y` force `annual`, `P1M` force `monthly`; tests couvrent les conflits `packageType` vs periode Store | Fait |
| Scope commit pour build Play | `git add --dry-run ...` sur la liste du runbook | Valide le 2026-05-10; tous les chemins RevenueCat QA existent et aucun fichier n'a ete stage pendant le dry-run |

## Commandes de verification actuelles

```bash
npm run subscription:qa:report
npm run subscription:qa:verify-local
npm run subscription:qa:device-app-user-id -- --device emulator-5554 --env-file .env.teststore
npm run android:device:physical
npm run android:play-qa-device -- --device <adb-id>
npm run android:play-qa-device:wait
npm run subscription:qa:release-gate
npm run android:gates:strict
npm test -- scripts/subscription-qa-report.test.js scripts/update-subscription-qa-evidence.test.js scripts/update-revenuecat-play-store-state.test.js scripts/update-google-play-subscription-state.test.js scripts/update-google-cloud-project-state.test.js scripts/update-google-oauth-android-client-state.test.js scripts/update-google-play-payments-profile-state.test.js scripts/update-supabase-play-integrity-secrets-state.test.js scripts/run-subscription-teststore-purchase.test.js scripts/run-subscription-account-switch.test.js scripts/verify-subscription-qa-local.test.js scripts/check-android-release-gates.test.js scripts/check-android-adb-device.test.js scripts/check-play-install-source.test.js scripts/check-play-qa-device.test.js scripts/wait-for-play-qa-device.test.js scripts/extract-revenuecat-app-user-id.test.js --runInBand --watchman=false
npm run lint
npm run typecheck:app
```

Etat verifie au 2026-05-15:

- `subscription:qa:report`: passe
- `subscription:qa:report`: relance le 2026-05-15T00:48Z; affiche `Current Session Readiness`, 8 scenarios automatises, 6 scenarios manuels/externes verifies (`test_store_monthly`, `test_store_annual`, `restore_after_reinstall`, `account_switch`, `play_monthly`, `play_annual`) et 1 gate restante (`play_cancellation_and_expiry`). Les variables Test Store sont absentes, l'approbation d'achat n'est pas definie, le POCO physique est visible via ADB Wi-Fi et l'app est Play-installed, Google Play direct confirme `monthly/P1M/ACTIVE` et `annual/P1Y/ACTIVE`, le snapshot RevenueCat Play mensuel affiche encore `prodfce10ef2a8: annual/P1Y; expected P1M`, et le snapshot annuel est `READY` en `prod98337b31be: annual/P1Y`.
- `revenuecat-qa-evidence.local.json`: inspection locale non sensible relancee le 2026-05-15; `play_monthly` et `play_annual` sont `passed` avec `appUserId`, `easBuildId`, `deviceId=192.168.1.116:41183`, `installerPackageName=com.android.vending`, `versionCode=24` et texte de preuve non vide. `play_cancellation_and_expiry` reste `pending`, sans `testedAt`, `tester`, `appUserId` ni `easBuildId`.
- `subscription:qa:verify-local`: passe le 2026-05-14T22:55Z, avec 17 suites et 165 tests du harnais QA scripts; il inclut maintenant `check-android-release-gates.test.js`, les snapshots Google OAuth, profil de paiement Play et secrets Supabase Play Integrity, la readiness Play monthly/annual, le wait helper Play device, les exigences Play annual `P1Y`, `versionCode` Play et cancellation/expiry webhook/backend a l'ecriture des preuves, et son sous-test release gate isole volontairement les preuves locales et attend donc encore le blocage "7 gates" sans fichier local
- `subscription:qa:release-gate`: relance le 2026-05-15T00:50Z; echoue volontairement avec 1 gate restante, `play_cancellation_and_expiry`, car la preuve locale contient maintenant les preuves `play_monthly` et `play_annual` mais pas encore la convergence cancellation/expiry webhook/backend.
- `android:gates:strict -- --report-only`: relance le 2026-05-15T00:48Z; echoue volontairement tant que `subscription:qa:release-gate` est rouge. Resultat courant: 10 pass, 1 fail (`RevenueCat subscription QA release gate`), 1 blocked (`Play payments profile for Billing`: `tax_information/missing/critical`, `payout_method/missing/critical`, `ireland_tax_information/missing/warning`), 2 manual (`Supabase Play Integrity secrets`, `Play-installed RevenueCat purchase and restore`). Le gate ADB device passe maintenant avec le POCO visible, donc le blocage restant est Play/RevenueCat/paiement, pas la connexion du telephone.
- `android:play-qa-device -- --device 192.168.1.116:41183 --report-only`: la relance historique du 2026-05-14T22:55Z voyait le POCO Play-installed en `versionCode=12`; apres opt-in internal et update Play, les relances du 2026-05-15 confirment `versionCode=24`, `versionName=1.2.0`, `installerPackageName=com.android.vending` et `expectedVersionCode: PASS`. Le blocage courant n'est plus le build installe, mais l'UI verrouillee (`SCREEN_STATE_OFF` / `INTERACTIVE_STATE_SLEEP` / `uiKeyguardShowing: true`) quand `--require-ui-ready` est demande.
- tests scripts QA: passent, dont le refus des preuves qui gardent le texte du template, une date invalide, une identite vide/en espaces, un `appUserId` non UUID ou un `easBuildId` non UUID dans le report et le helper d'ecriture, plus le blocage du gate Android quand le profil de paiement Play a des exigences ouvertes
- tests cibles abonnement: `services/__tests__/revenuecatUI.test.ts`, `services/__tests__/subscriptionService.test.ts`, `services/subscriptionSyncService.test.ts`, `services/mocks/subscriptionServiceMock.test.ts`, `hooks/useSubscription.test.tsx`, `lib/__tests__/revenuecat.test.ts`, `lib/__tests__/revenuecatSubscriber.test.ts`, `tests/app-routes/settingsScreen.test.tsx` repassent le 2026-05-14 (8 suites, 93 tests)
- lint/typecheck app: repassent le 2026-05-14 apres les derniers commits QA (`npm run lint`, `npm run typecheck:app`)
- `typecheck:tests`: echoue actuellement sur des tests legacy hors perimetre RevenueCat (`DreamMutation`, mocks non types, parametres implicites `any`); ne pas l'utiliser comme preuve de completion du workflow abonnement tant que ces tests globaux ne sont pas remis a niveau
- EAS Play Store profiles: `release`, `preview`, `production`, `production-apk` utilisent la cle `goog_`
- canari mock QA Lab Android: passe sur `emulator-5554`
- canari Test Store readiness Android: repasse le 2026-05-14 sur `emulator-5554`; `RevenueCat Test Store`, `PACKAGES=2`, `Buy monthly`, `Buy annual`, `$9.99`, `$79.98`
- garde achat Test Store: `npm run test:e2e:subscription-teststore:purchase -- --plan monthly --device emulator-5554` refuse sans approbation explicite
- achat Test Store monthly local: passe le 2026-05-14 avec `REVENUECAT_QA_AUTH=google`, `REVENUECAT_QA_APPROVAL=I_APPROVE_TEST_STORE_PURCHASE`, `--plan monthly`, `--device emulator-5554`; le flow confirme `TEST VALID PURCHASE`, `monthly purchase completed`, `Refresh completed`, `plus / active`
- achat Test Store annual local: passe le 2026-05-14 avec `REVENUECAT_QA_AUTH=google`, `REVENUECAT_QA_APPROVAL=I_APPROVE_TEST_STORE_PURCHASE`, `--plan annual`, `--device emulator-5554`; le flow confirme `TEST VALID PURCHASE`, `annual purchase completed`, `Refresh completed`, `plus / active`, et le snapshot affiche `product yearly`, `renews yes`
- restore Test Store apres reinstall local: passe le 2026-05-14 apres `adb uninstall com.tanuki75.noctalia` puis `ANDROID_SERIAL=emulator-5554 npx expo run:android`; `env QA_EMAIL=... node ./scripts/run-maestro-android.js --flow maestro/subscription-teststore-restore-google-manual.yml --env-file .env.teststore --device emulator-5554 --retries 0 --no-restart-metro` confirme Google Sign-In, `Restore completed`, `Refresh completed`, `plus / active`, snapshot `product yearly`, `renews yes`
- reconnexion Google locale: refaite le 2026-05-14 sur `Pixel_7_Play_API_36` via `Continue with Google`; la feuille native a selectionne le compte Google testeur et l'ecran Settings affiche ensuite `You're signed in and syncing` avec le QA Lab `RevenueCat Test Store`
- guard logout/no-leak local: passe le 2026-05-14 avec `node ./scripts/run-maestro-android.js --flow maestro/subscription-teststore-signout-guard.yml --env-file .env.teststore --device emulator-5554 --retries 0 --no-restart-metro`; apres sign-out, `Continue with Google` est visible et les textes `Noctalia Plus is active` / `plus / active` sont absents. Cela ne remplace pas la preuve `account_switch` a deux comptes reels.
- account-switch Test Store local: passe le 2026-05-14 apres creation et confirmation email d'un second compte alias Gmail. Le compte principal est d'abord revenu `plus / active` via achat Test Store monthly, puis le second compte email/password s'est connecte et QA Lab a affiche `free (free / inactive)`, `product none`, `3 analysis left`, `auth ready`. La preuve locale `account_switch` est enregistree avec l'appUserId RevenueCat du second compte `62d96407-ca3c-4493-9c79-5cd8d300d197`.
- extraction appUserId device: `npm run subscription:qa:device-app-user-id -- --device emulator-5554 --env-file .env.teststore` passe le 2026-05-14; le helper lit les preferences RevenueCat via `adb shell run-as`, masque la cle `test_z...JPrz` et retourne `1239729f-7468-48c9-b26a-7aa8b4a82591`, identique aux preuves locales Test Store.
- MCP RevenueCat live: la session courante renvoie toujours `403 access token has been revoked` lors de `get_product_store_state`, `get_customer`, `list_subscriptions` et `list_purchases`; tentative `codex mcp login revenuecat` le 2026-05-14 ouverte dans le navigateur puis interrompue faute de callback OAuth. Un `codex exec` frais le 2026-05-14T15:36Z a toutefois pu appeler `get_product_store_state` en lecture seule pour les produits Play et confirmer le lag `prodfce10ef2a8 -> annual/P1Y/ACTIVE`. Retest le 2026-05-15 depuis cette session locale: `codex exec --ephemeral` echoue avant MCP avec `attempt to write a readonly database` puis `failed to initialize in-process app-server client: Operation not permitted`; nouvelle tentative `codex mcp login revenuecat` rejetee par la limite d'usage Codex avant ouverture OAuth. Nouvelles sondes `get_customer` a 02:37 puis apres le dernier audit: meme `403 access token has been revoked`. Nouvelle tentative OAuth a 03:38 Europe/Paris: URL ouverte dans Chrome, mais aucun callback local n'est revenu; le processus a ete arrete proprement. Il faudra reauth RevenueCat MCP ou relancer une session Codex avec acces state DB avant de pouvoir relire le backend apres achat Play.
- MCP RevenueCat entitlement: `Noctalia Plus` (`entla0745c9b44`) attache les quatre produits attendus
- Expo MCP build Android: verification relancee le 2026-05-10; dernier Store build fini `310244ed-027b-4028-8522-70c0f676a0e9`, mais insuffisant pour valider Play Internal Testing du workflow actuel
- Play Internal Testing: build Android Store `ddbc756d-8db6-4337-80fa-68cc86f8b62a` cree depuis `c19bf249` avec la cle `goog_`; auto-submit Internal Testing `99d2a4b1-3eac-4479-850f-bf179c13af91` confirme `FINISHED`, track `internal`, release `COMPLETED`. Relecture Play Developer API le 2026-05-15 via un edit temporaire non commite confirme aussi `track=internal`, release `1.2.0`, `versionCodes=["24"]`, `status=completed`. Le manifest du build 24 marque camera/micro optionnels et bundletool genere des splits APK pour l'emulateur, mais Google Play affiche encore `Your device isn't compatible with this version` sur `Pixel_8_Play_API_36` puis sur `Pixel_6_Play_API_35` apres login Google MFA valide. Le Device Catalog Play Console montre que `emu64a` n'existe pas dans `Tous les appareils`, tandis que seul `google emu64xa` / Intel est compatible; le blocage restant est donc cote Play/device catalog pour l'AVD Apple Silicon, et la validation Play doit passer par un appareil physique compatible ou par une cible Play Console explicitement compatible.
- Device tiers API: `applications.deviceTierConfigs.list` avec l'ADC Android Publisher et `x-goog-user-project: gen-lang-client-0336445544` renvoie `204 No Content`, donc aucune device tier config API ne ressort comme cause du filtrage AVD.
- Filtres manifest Play APK: `aapt` sur l'APK universel Play confirme SDK 33-36, ABI `arm64-v8a` incluse, ecrans/densites compatibles et camera/micro optionnels. L'emulateur `emulator-5554` expose API 36, `arm64-v8a`, 1080x2400, densite 420; le blocage Play ne vient donc pas d'un filtre manifest evident.
- Device spec POCO: le 2026-05-15, `bundletool get-device-spec --device-id=192.168.1.116:41183` confirme `supportedAbis=["arm64-v8a"]`, `sdkVersion=36`, `screenDensity=480`, `supportedLocales=["fr-FR"]`, `buildBrand=POCO`, `buildDevice=myron`. `bundletool build-apks --bundle=/private/tmp/noctalia-v24.aab --device-spec=/private/tmp/noctalia-poco-25102pcbeg-device-spec.json --output=/private/tmp/noctalia-v24-poco-25102pcbeg.apks --overwrite` genere les splits `base-arm64_v8a`, `base-fr`, `base-master`, `base-xxhdpi` pour ce telephone reel. Cette preuve ne ferme aucune gate Play, mais elle reduit fortement l'hypothese d'un binaire v24 incompatible avec le POCO.
- Play generated APKs: l'API officielle `generatedApks.list` sur le versionCode 24 renvoie un APK universel et des splits generes par Play App Signing, donc l'AAB est bien transforme cote Play. L'APK universel telecharge pese 131M, porte `versionCode=24`, declare `com.android.vending.BILLING`, et `apksigner` confirme le certificat Play App Signing SHA-256 `6a8cb2e2cdd2c1fdd7c5cfcf00d7c3cc5861c2cd3faf49f69a312535a44fee0f` et SHA-1 `bccfc296384781d68cb7b65aba84cbb38c85e059` (`BC:CF:C2:96:38:47:81:D6:8C:B7:B6:5A:BA:84:CB:B3:8C:85:E0:59` en format Google Cloud). Cette preuve ne ferme pas l'installation Internal Testing, car elle ne dit pas pourquoi les AVD restent filtres ni si le client OAuth Android Google Cloud contient bien ce SHA-1.
- Play Console UI + API: acces lecture seule confirme le 2026-05-14 dans le compte developpeur `TiMax group`; `Noctalia: Smart Dream Journal` est en Production, `Monetiser avec Play -> Abonnements -> Noctalia Plus` liste `annual` (`Annuel, renouvellement automatique`) et `monthly` (`Mensuel, renouvellement automatique`) comme forfaits actifs. Apres reauth ADC Android Publisher, l'API officielle `subscriptions.get` confirme aussi `monthly/P1M/ACTIVE` et `annual/P1Y/ACTIVE` pour `productId=noctalia_plus`. Cette preuve confirme que le base plan mensuel existe cote Play, mais ne ferme pas `play_monthly` car RevenueCat MCP lit encore `prodfce10ef2a8` comme `annual/P1Y`.
- EAS env cloud: `npm_config_cache=/Users/tanuki/Documents/dreamer/.npm-cache npx eas-cli env:list --environment production|preview|development` confirme que les trois environnements exposent `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`, `EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID`, `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`, `EXPO_PUBLIC_PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER`, Supabase et RevenueCat. L'absence du Google Web Client ID dans certains profils `eas.json` n'est donc pas un blocage si l'environnement EAS reste attache.
- Google OAuth Android Play: Google Cloud Console Auth Platform confirme le client Android `Noctalia Android Production` avec package `com.tanuki75.noctalia`, client id `359653779023-5dhs012rh7l3cjf0leoknn7j0dlgq0ok.apps.googleusercontent.com`, SHA-1 Play `BC:CF:C2:96:38:47:81:D6:8C:B7:B6:5A:BA:84:CB:B3:8C:85:E0:59` et derniere utilisation `11 mai 2026`. Snapshot local gitignore `doc_web_interne/docs/google-oauth-android-client-state.local.json` mis a jour via `npm run android:google-oauth-android-client-state`.
- Profil de paiement Play: Play Console affiche un probleme de profil de paiement; snapshot local gitignore `doc_web_interne/docs/google-play-payments-profile-state.local.json` cree le 2026-05-14 via `npm run android:google-play-payments-profile-state`. Il contient trois exigences ouvertes: informations fiscales, mode de paiement valide et informations fiscales Irlande. `android:gates:strict` bloque maintenant ce point avant publication production.
- Emulateur alternatif: `Pixel_7_Play_API_36` cree et boote le 2026-05-14 avec Play Store present; compte `thannous@gmail.com` connecte apres validation 2FA sur POCO. Recheck via `market://details?id=com.tanuki75.noctalia` et `adb exec-out uiautomator dump /dev/tty` le 2026-05-14: la fiche Play Noctalia affiche l'app Beta, le statut testeur, `This app is available only for your other devices`, `POCO 25102PCBEG` en `Installed` et `HONOR PGT-N19` avec `Install`; le modele Pixel 7 ne contourne donc pas le filtrage Play, mais les telephones du compte restent les cibles plausibles pour finir les gates Play.
- Emulateur Play API 35: `system-images;android-35;google_apis_playstore;arm64-v8a` installee le 2026-05-14, AVD `Pixel_6_Play_API_35` cree puis lance. Android 15 et Play Store sont OK; la reprise du 2026-05-14T20:42Z valide le compte Google apres MFA telephone avec le code `62`, `dumpsys account` affiche `Account {name=thannous@gmail.com, type=com.google}`, et le backup Google de l'emulateur est desactive avant acceptation des services. La fiche Play Noctalia beta s'ouvre ensuite, confirme le statut testeur, puis affiche `Your device isn't compatible with this version`; l'API 35 ne suffit donc pas pour obtenir une installation Play Internal Testing.
- Recheck Play Store AVD: le 2026-05-14T05:12:28Z, ouverture de `market://details?id=com.tanuki75.noctalia` sur `emulator-5554` affiche `Noctalia: Smart Dream Journal (Beta)`, `You're a beta tester for this app`, mais aussi `This app isn't compatible with your device anymore`. La fiche propose `Open/Uninstall` car un build local est deja installe; `dumpsys package com.tanuki75.noctalia` confirme `versionCode=1`, `flags=[ DEBUGGABLE ... ]`, `initiatingPackageName=com.android.shell` et `installerPackageName=null`. Cette installation ne ferme donc aucune gate Play.
- Recheck appareil physique / ADB Wi-Fi: le 2026-05-14T05:14:44Z, `adb devices -l` ne voit que `emulator-5554`, `adb mdns services` ne decouvre aucun service wireless debugging, et `system_profiler SPUSBDataType` ne remonte aucun appareil Android/USB exploitable. La validation Play sur POCO/HONOR ne peut donc pas etre pilotee localement depuis ce Mac tant qu'un appareil compatible n'est pas branche ou apparie en ADB Wi-Fi.
- Sideload build 24: les splits generes depuis l'AAB `ddbc756d-8db6-4337-80fa-68cc86f8b62a` doivent etre signes pour un sideload local; avec une keystore debug temporaire, `bundletool install-apks --apks=/private/tmp/noctalia-ddbc756d-signed.apks --device-id=emulator-5554` installe l'app, `dumpsys package` confirme `versionCode=24`, et `am start` affiche l'ecran Noctalia. Cette preuve confirme le lancement du binaire, pas l'installation Play ni les achats Play.
- Sideload APK universel Play: `adb install /private/tmp/noctalia-play-universal-v24.apk` reussit apres desinstallation du build local, mais `installerPackageName=null` et le lancement bascule vers `com.pairip.licensecheck.LicenseActivity` avec l'ecran Play Store `Get this app from Play`; ce chemin ne permet donc pas de remplacer l'installation Internal Testing.
- Android physique: `npm run android:device:physical` relance le 2026-05-14T21:40Z confirme que `adb` est installe et disponible, mais aucun appareil n'est visible pour adb (`ADB DEVICE: MISSING - No device is visible to adb.`). macOS USB voit maintenant `POCO F8 Ultra, Xiaomi, serial 57275d36`, `idVendor 10007`, `idProduct 65352`, et `Android debug interface signature present`; Wireless Debugging reste non visible. `ADB_TRACE=usb adb start-server` initialise `libusb`, mais ne loggue aucun transport pour le POCO; `adb usb` repond `error: no devices/emulators found`. Le workaround local `~/.android/adb_usb.ini` a ete normalise avec `0x2717` et `0x18d1`, puis ADB redemarre; `adb devices -l` reste vide. Il faut donc deverrouiller le POCO, accepter/recreer l'autorisation RSA USB debugging, ou activer Wireless debugging et garder l'ecran d'appairage ouvert avant de reprendre les validations appareil reel.
- RevenueCat MCP store state: lecture fraiche le 2026-05-14T15:36Z via `codex exec` frais et `get_product_store_state` sur `prodfce10ef2a8` et `prod98337b31be`. Le produit mensuel RevenueCat reste `prodfce10ef2a8`, mais son store-state detaille remonte encore `annual/P1Y/ACTIVE`; le produit annuel `prod98337b31be` remonte aussi `annual/P1Y/ACTIVE`. Le rapport QA garde cette incoherence visible en `LAGGING` car Google Play direct est `monthly/P1M/ACTIVE`, et ne la laisse pas masquer les vraies gates Play-installed/backend.
- RevenueCat Play Store snapshot: `doc_web_interne/docs/revenuecat-play-store-state.local.json` contient une lecture MCP resume rafraichie le 2026-05-14T15:37:16Z et reste gitignore. `subscription:qa:report` affiche `Play monthly base plan snapshot` en `LAGGING` tant que Google Play direct est pret mais RevenueCat store-state lag.
- Evidence Play install source: `subscription:qa:evidence` et `subscription:qa:report` refusent maintenant une preuve `play_*` qui ne confirme pas une installation Play Internal Testing via `installerPackageName=com.android.vending` en champ structure et via `com.android.vending` ou `installed from Play` dans le texte, afin que le sideload Play-signe ne puisse pas fermer les gates Play. Le helper `npm run android:play-install-source -- --device <adb-id>` ajoute une verification ADB directe de `installerPackageName=com.android.vending` avant d'enregistrer une preuve Play.
- Google Play subscription snapshot: `doc_web_interne/docs/google-play-subscription-state.local.json` contient une lecture API officielle rafraichie le 2026-05-14T15:21:48Z et reste gitignore. `subscription:qa:report` affiche `Google Play monthly base plan snapshot` en `READY` (`monthly/P1M/ACTIVE`) et `Google Play annual base plan snapshot` en `READY` (`annual/P1Y/ACTIVE`) tout en signalant le `Play monthly base plan snapshot` RevenueCat en `LAGGING` et le `Play annual base plan snapshot` RevenueCat en `READY`.
- Google Play internal track snapshot: `doc_web_interne/docs/google-play-track-state.local.json` contient une lecture API officielle rafraichie le 2026-05-14T23:12:00Z via `edits.tracks.get` puis suppression de l'edit sans commit, et reste gitignore. `subscription:qa:report` affiche maintenant `Google Play internal track snapshot` en `READY` (`internal/1.2.0/completed/versionCode=24`), et `android:gates:strict -- --report-only` affiche `Google Play internal track release` en `PASS`.
- scope de commit Play: premier commit cree le 2026-05-15 (`0068ce37e docs(revenuecat): record Play QA validation status`) avec les deux docs QA du statut Play. Deuxieme commit cree ensuite (`25b1dd1ee test(revenuecat): add subscriber expiry snapshot check`) avec le helper de snapshot RevenueCat Subscriber API, ses tests, le rapport QA et la doc. Les autres changements assets/docs/maestro restaient volontairement hors staging.
- preuve locale: `doc_web_interne/docs/revenuecat-qa-evidence.local.json` present mais gitignore; `subscription:qa:report` du 2026-05-15 affiche maintenant 6 scenarios verifies (`test_store_monthly`, `test_store_annual`, `restore_after_reinstall`, `account_switch`, `play_monthly`, `play_annual`) et 1 gate Play restante (`play_cancellation_and_expiry`). `subscription:qa:release-gate` doit rester rouge tant que le webhook/backend cancellation-expiry n'est pas relu.
- gate Android strict: `npm run android:gates:strict -- --report-only` relance le 2026-05-15 avant achat affichait `10 pass, 1 fail, 1 blocked, 2 manual`; le blocked production reste `Play payments profile for Billing` avec `tax_information`, `payout_method` et `ireland_tax_information` ouverts. Ce resultat interdit une publication Android production meme apres les achats Play test mensuel/annuel.
- preflight Play Billing POCO: le 2026-05-15, `npm run android:play-qa-device -- --report-only` passe sur `192.168.1.116:41183` avec `installerPackageName=com.android.vending`; le paywall Play affiche les prix `3,59 EUR/mois` et `22,99 EUR/an`. L'ouverture des feuilles mensuelle et annuelle montre `Carte test, toujours approuvee` et `Il ne vous sera pas facture`; les prix acceleres affiches sont `3,59 EUR/5 min` et `22,99 EUR/30 min`. Les achats test mensuel et annuel ont ensuite ete valides depuis cette installation Play v24; les preuves locales d'ecran/API/logs sont dans `/private/tmp/noctalia-poco-v24-monthly-*`, `/private/tmp/noctalia-play-monthly-*`, `/private/tmp/noctalia-poco-v24-annual-*` et `/private/tmp/noctalia-play-annual-*`. L'appUserId relu via logcat reste `1239729f-7468-48c9-b26a-7aa8b4a82591`.
- cancellation/expiry Play Billing: le 2026-05-15, les abonnements test mensuel puis annuel ont ete annules via Google Play Developer API apres achat Play test. Les relectures `purchases.subscriptionsv2.get` ont confirme `SUBSCRIPTION_STATE_EXPIRED`, `ACKNOWLEDGEMENT_STATE_ACKNOWLEDGED`, `testPurchase: true`, `developerInitiatedCancellation`, avec `basePlanId=monthly` pour le mensuel puis `basePlanId=annual` pour l'annuel. Les preuves store locales sont `/private/tmp/noctalia-play-monthly-after-expiry-v2.json` et `/private/tmp/noctalia-play-annual-after-expiry-v2.json`; la capture `/private/tmp/noctalia-poco-v24-annual-after-expiry-before-restore.png` existe mais est noire, donc elle prouve surtout l'ecran eteint/verrouille et ne remplace pas une capture UI Noctalia lisible. Apres expiration mensuelle, `Restaurer les achats` dans l'app est revenu a `restoredTier: 'free'`, `isActive: false` et l'UI free/paywall. Apres expiration annuelle, l'API Play est bien expiree mais la derniere restauration app reste a faire: le POCO est repasse sur l'ecran verrouille (`NotificationShade`, `mDreamingLockscreen=true`), donc il faut le deverrouiller avant de relancer `Restaurer les achats` et capturer le retour app. La gate `play_cancellation_and_expiry` reste donc ouverte, surtout faute de relecture webhook/backend RevenueCat. Une recherche repo confirme que la preuve backend equivalente devrait passer par `public.subscription_state`, `public.subscription_events`, `/subscription/refresh`, `/subscription/reconcile` ou `supabase/functions/revenuecat-webhook/index.ts`; `/subscription/refresh` exige un utilisateur authentifie, `/subscription/reconcile` exige `REVENUECAT_RECONCILE_SECRET`, et la session locale ne contient aucune variable `SUPABASE*`, `REVENUECAT*`, `EXPO_PUBLIC_SUPABASE*` ou `EXPO_PUBLIC_API*`, donc cette voie backend directe n'est pas interrogeable sans reauth/acces live.
- EAS build installe sur POCO: l'etat initial Play-installed du 2026-05-15 etait `versionCode=12`, `versionName=1.1.0`, `installerPackageName=com.android.vending`; `eas build:list --platform android` rattache ce binaire au build EAS `9df05e30-0569-4f7c-8af9-62c692fa4c3a` (`appBuildVersion=12`, `appVersion=1.1.0`, commit `20e47f0c553faae8e83d9d8f0726d473320ac217`). La relecture `edits.tracks.list` montre que `production` sert `versionCode=12`, que `beta` sert `11`, et que seule `internal` sert `24`; la Play Console confirme aussi que `thannous@gmail.com` est dans une liste cochee de testeurs internal. Apres acceptation de l'invitation internal, force-stop du Play Store et `Mettre a jour`, `npm run android:play-qa-device:wait -- --device 192.168.1.116:41183 --expected-version-code 24 --require-ui-ready` passe avec `versionCode=24`, `versionName=1.2.0` et `installerPackageName=com.android.vending`. La validation release robuste doit donc utiliser le build Internal Testing `ddbc756d-8db6-4337-80fa-68cc86f8b62a` (`versionCode=24`, `versionName=1.2.0`) pour l'achat final.
- Recheck session POCO: le 2026-05-14T22:55Z, `npm run android:play-qa-device -- --device 192.168.1.116:41183 --report-only` passe avec le binaire Play installe `versionCode=12`; le meme appareil reste Play-installed via `com.android.vending` et les commandes de preuve incluent `--version-code 12`. Le strict `--expected-version-code 24` reste rouge tant que Play ne met pas a jour l'app. L'ouverture depuis la fiche Play amene au paywall avec les deux produits charges (`Mensuel 3,59 EUR`, `Annuel 22,99 EUR`) et le bouton `Continuer`. Les relectures RevenueCat MCP fraiches du 2026-05-14T22:56Z puis du 2026-05-14T23:16Z pour `appUserId=1239729f-7468-48c9-b26a-7aa8b4a82591` retournent `active_entitlements_count=0`, `sandbox_purchase_count=0`, `production_purchase_count=0`, `errors=[]`; aucune preuve `play_*` ne doit donc etre enregistree tant que l'achat Play n'a pas ete explicitement valide et observe.
- Etat UI appareil: le 2026-05-15T00:32Z, `dumpsys window` indique encore `mCurrentFocus=NotificationShade` et `mDreamingLockscreen=true` (ecran repasse `SCREEN_STATE_OFF` apres le reveil ADB). A 02:33 Europe/Paris, `npm run android:play-qa-device:wait -- --device 192.168.1.116:41183 --expected-version-code 24 --require-ui-ready --timeout-ms 5000 --interval-ms 1000` confirme que le POCO est bien Play-installed en `versionCode=24`, `installerPackageName=com.android.vending`, mais timeout car `uiScreenState=SCREEN_STATE_OFF`, `uiInteractiveState=INTERACTIVE_STATE_SLEEP`, `uiKeyguardShowing=true`. Nouvelle sonde `dumpsys window` a 02:37 Europe/Paris: `screenState=SCREEN_STATE_OFF`, `mCurrentFocus=NotificationShade`, `mDreamingLockscreen=true`. Reprise a 03:39 Europe/Paris: `adb shell input keyevent WAKEUP` rallume l'ecran, mais `secure=true`, `uiKeyguardShowing=true` et le wait helper reste en timeout sur keyguard. Les flows Play/Google pilotables par UI exigent donc que le POCO soit deverrouille avant de reprendre.
- Commande de reprise POCO: relance courte a 02:41 Europe/Paris de `npm run android:play-qa-device:wait -- --device 192.168.1.116:41183 --expected-version-code 24 --require-ui-ready --timeout-ms 2000 --interval-ms 1000`. La commande est valide et passe les prechecks Play (`physicalDevice: PASS`, ADB Wi-Fi visible, `versionCode=24`, `versionName=1.2.0`, `installerPackageName=com.android.vending`, `expectedVersionCode: PASS`), puis timeout uniquement sur `--require-ui-ready` avec `SCREEN_STATE_OFF`, `INTERACTIVE_STATE_SLEEP`, `uiKeyguardShowing=true`.
- Wake-only check: `adb shell input keyevent WAKEUP` relance apres les checks ci-dessus allume l'ecran (`SCREEN_STATE_ON`) mais garde `mCurrentFocus=NotificationShade` et `mDreamingLockscreen=true`; il ne suffit donc pas de reveiller l'appareil, il faut un deverrouillage utilisateur. Nouvelle sonde a 02:45 Europe/Paris: l'ecran est de nouveau `SCREEN_STATE_OFF`, avec `mCurrentFocus=NotificationShade`, `mFocusedApp=com.tanuki75.noctalia/.MainActivity` et `mDreamingLockscreen=true`.
- Logcat apres expiry annual: `adb -s 192.168.1.116:41183 logcat -d -T '05-15 02:28:00.000' | rg -i "restore|restoredTier|isActive|expired|expiry|subscription|convergence|tier|RevenueCat|free|plus|Noctalia"` relu apres l'expiration annuelle. Il montre Noctalia comme top task derriere `NotificationShade`, puis `SCREEN_OFF`, mais ne contient pas de log applicatif probant `restore`, `restoredTier`, `subscription_refresh` ou convergence free apres 02:28. Cette absence confirme qu'il ne faut pas enregistrer `play_cancellation_and_expiry` sans relancer l'action app ou relire le backend.
- Restore Play-installed sans achat: le 2026-05-14T22:41Z, action `Restaurer les achats` lancee sur le paywall du POCO Play-installed. Les logs app affichent `Purchase restore started`, l'avertissement RevenueCat `allowSharingPlayStoreAccount is set to false`, puis `Purchase restore completed` avec `restoredTier: 'free'` et `Tier reconciliation successful`. La relecture RevenueCat MCP fraiche du meme `appUserId` retourne encore `active_entitlements_count=0`, `sandbox_purchase_count=0`, `production_purchase_count=0`, `errors=[]`. Cette preuve valide l'absence de faux entitlement restore, mais ne ferme pas les gates `play_monthly`, `play_annual` ou `play_cancellation_and_expiry`.

Note: la session Codex deja ouverte peut conserver l'ancien token MCP jusqu'a reload. Pour une
verification RevenueCat live apres reauth, utiliser un nouveau `codex exec` ou redemarrer la session.

## Audit objectif au 2026-05-15T00:45Z

Objectif concret: prouver le workflow RevenueCat de bout en bout pour profils mock, Test Store,
switch de compte, achats Play Internal Testing monthly/annual, restore et expiration/cancellation,
sans depense reelle, puis ne publier Android production que si les gates sont robustes.

Checklist preuve actuelle:

| Exigence | Preuve actuelle | Statut |
| --- | --- | --- |
| Mock/free/plus/cancelled/expired | `maestro/subscription-qa-lab.yml`, `subscription:qa:report` | Couvert |
| Test Store monthly/annual | preuves locales `test_store_monthly`, `test_store_annual` | Couvert |
| Restore apres reinstall | preuve locale `restore_after_reinstall` | Couvert |
| Switch de compte sans fuite entitlement | preuve locale `account_switch` | Couvert |
| Play Internal Testing monthly | preuve locale `play_monthly`, POCO v24 Play-installed, Play API `monthly` expired ensuite | Couvert |
| Play Internal Testing annual | preuve locale `play_annual`, POCO v24 Play-installed, Play API `annual` expired ensuite | Couvert |
| Cancellation/expiry app + backend | Play API monthly/annual expired; RevenueCat subscriber API direct voit le Play sandbox expire; app restore free observe pour monthly seulement; annual app restore et Supabase/webhook non relus | Manquant |
| Commit utilisateur demande | commits `0068ce37e` et `25b1dd1ee`; autres changements workspace non lies conserves hors staging | Couvert |
| Publication Android production | `android:gates:strict -- --report-only` = 10 pass, 1 fail, 1 blocked, 2 manual | Interdite |

Conclusion: ne pas marquer l'objectif complet. Le prochain pas necessaire est deverrouiller le
POCO pour executer la sequence de reprise annual post-expiry, puis restaurer un acces Supabase ou
webhook/reconcile pour prouver la convergence app-backend.

Tentative backend supplementaire: la config client expose bien `EXPO_PUBLIC_API_URL`,
`EXPO_PUBLIC_SUPABASE_URL` et la cle anon publique Supabase dans les `.env*`, mais une tentative
Supabase password grant pour le compte testeur renvoie `400 Invalid login credentials`; il faut donc
un vrai jeton Google/Supabase actif depuis l'app ou un acces admin/reconcile pour appeler
`/subscription/refresh` en preuve backend. Extraction locale de session depuis le binaire Play non
possible non plus: `adb shell run-as com.tanuki75.noctalia ...` renvoie `package not debuggable`.
Le connecteur Gmail ne peut pas servir de secours pour l'invitation RevenueCat dans cette session:
la recherche Gmail renvoie `401 token_expired`, donc il faut aussi reauth Gmail avant d'accepter ou
relire une invitation par ce canal.

Preuve RevenueCat directe supplementaire: la cle SDK publique Android Play permet une lecture
read-only de `https://api.revenuecat.com/v1/subscribers/<appUserId>`. Lecture sanitisee sauvegardee
dans `/private/tmp/noctalia-revenuecat-subscriber-after-play-expiry.json`: HTTP 200, entitlement
`Noctalia Plus` / produit `noctalia_plus`, `expires_date=2026-05-15T00:28:06Z`,
`is_active_at_check=false`, subscription Play `store=play_store`, `is_sandbox=true`,
`unsubscribe_detected_at=2026-05-15T00:31:45Z`. Cette preuve couvre RevenueCat backend direct, mais
ne couvre pas encore le restore app annual post-expiry ni la convergence Supabase/webhook. Un essai
volontairement non enregistre de `subscription:qa:evidence --gate play_cancellation_and_expiry` avec
cette preuve echoue encore avec `Play cancellation evidence must confirm the RevenueCat webhook`.
Le validateur exige textuellement trois familles de signaux pour cette gate:
`cancel|cancellation|expiry|expired`, le mot `webhook`, puis `backend` avec `converg` ou `sync`.

Durcissement du 2026-05-15T01:31Z: ajout de
`scripts/update-revenuecat-subscriber-expiry-state.js`, du script npm
`subscription:qa:revenuecat-subscriber-expiry`, de
`doc_web_interne/docs/revenuecat-subscriber-expiry-state.example.json` et d'un snapshot local
gitignore. Le helper normalise la lecture RevenueCat Subscriber API v1 et refuse les entitlements
encore actifs ou les achats Play non-sandbox. `subscription:qa:report` affiche desormais
`RevenueCat subscriber expiry snapshot` en readiness separee, avec le detail explicite que cette
preuve directe ne remplace pas la convergence webhook/backend. `npm run subscription:qa:verify-local`
passe et couvre ce nouveau helper; le rapport force un fichier d'evidence manquant temporaire dans
ce verificateur pour rester stable meme quand les preuves locales Play monthly/annual existent deja.

Fallback emulateur du 2026-05-15T02:01Z: apres blocage du POCO sur keyguard securise, un APK
release local autonome a ete genere avec `.env.teststore` via `./gradlew --no-daemon
-Dorg.gradle.jvmargs='-Xmx6g -XX:MaxMetaspaceSize=1536m -Dfile.encoding=UTF-8'
app:assembleRelease`, puis installe uniquement sur `emulator-5554` depuis
`android/app/build/outputs/apk/release/app-release.apk`. L'app se lance sans Metro, Google Sign-In
selectionne `thannous@gmail.com`, et le QA Lab affiche `RevenueCat Test Store`, `PACKAGES=2`,
`USER=thannous@gmail.com`, `free (free / inactive)`, `PRODUCT=none`, `EXPIRY=none`,
`SERVER VERSION=182`, `AUTH=ready`, puis `Refresh completed` avec
`appUserId 1239729f-7468-48c9-b26a-7aa8b4a82591`. Logcat confirme `[useSubscription] Refresh
completed` avec `tier: 'free'`, `isActive: false`, puis `Subscription convergence started` en
`source: 'manual_refresh'`, `targetTier: 'free'`, `currentTier: 'free'`, `expiryDate: null`. Captures
locales: `/private/tmp/noctalia-emulator-after-google-account.png` et
`/private/tmp/noctalia-emulator-qa-lab-refresh-result.png`. Cette preuve ferme le fallback local
Test Store/backend-refresh du meme appUserId, mais ne remplace toujours pas la preuve
Play-installed POCO post-expiry ni la relecture webhook/Supabase.

Tentative de reprise sans POCO du 2026-05-15T09:10+02: `Pixel_7_Play_API_36` a ete relance sans
fenetre mais ne contient plus `com.tanuki75.noctalia`. `Pixel_8_Play_API_36` contient une build
debug `com.tanuki75.noctalia` avec `run-as` disponible, mais c'est une Development Build
`versionCode=1`, `installerPackageName=null`, ouverte sur Expo Dev Launcher. Le stockage local ne
contient pas de session Supabase du compte Play: `RKStorage` ne liste que des cles guest/mock, et
`SecureStore.xml` ne contient que `guest-session-v1` et `device-fingerprint-v1` chiffres. Les
preferences RevenueCat de cet emulateur correspondent a la cle Test Store et a un utilisateur
anonyme `$RCAnonymousID:4361d8c404204a8daa14e6c8df93da13`, sans abonnement. Cette piste ne peut donc
pas prouver la convergence backend du compte Play `1239729f-7468-48c9-b26a-7aa8b4a82591`.

## Conditions pour declarer l'objectif complet

Ne pas declarer l'objectif complet tant que ces sept portes ne sont pas passees dans `revenuecat-qa-evidence.local.json`:

- `test_store_monthly`
- `test_store_annual`
- `restore_after_reinstall`
- `account_switch`
- `play_monthly`
- `play_annual`
- `play_cancellation_and_expiry`

Chaque porte doit contenir `status: "passed"`, un `testedAt` valide, `tester`, un `appUserId` UUID et
`evidence`. Le champ `evidence` doit decrire le test reel observe, pas reprendre le texte d'exemple.
Les portes `play_*` doivent aussi contenir un `easBuildId` UUID, un `versionCode` entier positif,
un `deviceId` physique non-emulateur et `installerPackageName: "com.android.vending"` pour lier la
preuve au build installe via Play Internal Testing.
Le release gate doit ensuite passer:

```bash
npm run subscription:qa:release-gate
npm run android:gates:strict
```
