# TI-598 — Android real chat qualification, 10 September 2026

Status: **partial; not ready to close**. TI-506 and TI-599 remain separate qualification work.

## Baseline identity and scope

Source `c6db61081`, isolated branch `codex/ti598-real-chat-20260910`. Motorola, base package `com.tanuki75.noctalia`, local debug 3.1.0 (54), installer null, Metro on 8085 using the complete Play Store runtime profile. This is not a Play-distributed build. Only the owner's synthetic dream was used. Existing voice draft and unrelated repository work were preserved.

The original synthetic transcript differs from the historical TI-559 fixture; see `ti598-android-2026-09-10/fixture.json`. The 20 follow-up prompts reuse that fixture's conversational scenarios.

## Real run and persistence

- 20 nonempty visible assistant replies, 40 persisted messages, 20 persisted thought signatures (opaque values omitted from evidence).
- Database turn state: 20 `succeeded` rows, 21 total attempts, maximum 2 attempts for a turn.
- First attempt at turn 3 returned HTTP 500 Stream error. No empty assistant message was persisted. One explicit UI Retry succeeded without duplicating the user turn. This is not 20 error-free first attempts.
- Current failure's root cause remains unknown. The historical turn-7 failure was a completed thought-only output without visible text; that finding must not be transferred to this error without evidence.
- Original transcript and interpretation MD5 values match their pre-run values.
- Last-turn recall retained the grandmother association and rejected the work association.
- After a cold start on the local Release, UI scrolling observed every exact persisted message (positions 1–40) across seven snapshots. The last reply and initial-association recall were retained. See release-history-scroll.json.
- Native owner-to-guest isolation passed: after logout the synthetic chat link displayed not-found and no chat content. Google sign-in to the same owner restored access. This does not qualify switching between two authenticated accounts.

## Latency before the display fix

19 observable successful first attempts: median visibility upper bound 3750 ms, nearest-rank p95/max 5422 ms. None of these upper bounds is <= 2000 ms. The successful retry of turn 3 has no timed frame series.

These are conservative visibility bounds from timestamped ADB PNG captures and local Apple Vision OCR, not exact first-glyph or provider TTFT measurements. Capturing frames adds device overhead. Matching uses the first 12 normalized reply characters, increased to 18/16 for turns 10/13 to avoid matching older visible text. Per-turn bounds and previous frame times are in `visible-timing.json`. Provider latency, cost, and first-network-delta timing are not separately qualified. The 19/20 <= 2 s working target is not validated.

## Display correction and validation

Streaming text previously restarted a timer and blanked the text on every growing chunk. Display now shows each received plain-text chunk immediately. Final history replaces an identical virtual streaming bubble while local persistence is pending. Existing Android Markdown switching safeguards remain.

A focused UI regression fails against the original MessagesList and passes with the correction (2 tests). Lint on both changed production files: 0 errors, 10 existing warnings. Independent Bugbot review found no bugs; route-level deduplication and auto-sync/retry lack direct regression tests.

Isolated `npm ci` resolved the missing shared HealthKit dependency. The existing Lucid destination normalizer now explicitly accepts raw string pathnames in its type signature; runtime behavior is unchanged. Full application typecheck passes. Native Release build and cold-start/history checks pass; CI, merge, and Play delivery are separate states. No backend deployment, EAS build, or Store publication was performed.

## Product-language observation

Turn 13 called the later blue-gray correction part of the original account. Turn 15 again distinguished the rainy Sunday association from dream content. Persistent original data stayed unchanged. This semantic observation belongs to TI-559 quality follow-up, not proof of a storage mutation.

## Local Release checkpoint

Canonical `npm run android:release:local -- --profile production-apk --abi arm64-v8a` succeeded in 5m05 after isolated npm ci. APK SHA-256 `4b1faf900978b50749a06129ba65b8c6d4dcc6038b7ee1d39c26acf11e727605`. Package/version/certificate matched the installed base app; adb install -r succeeded without uninstall or data clearing. Native flags omit DEBUGGABLE. First cold deep-link launch reported 953 ms and displayed the synthetic chat. This is a local Release signed with the existing debug certificate; it is not Play distribution.

The current source chat default is gemini-3.5-flash-lite; a live provider/model override was not independently read from production configuration during this run. Do not infer model identity or cost solely from the local default. No fresh twenty-turn native run was performed after the display correction.
