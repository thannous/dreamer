# TI-506 — Local Release and Play boundary, 10 September 2026

Status: **blocked for Play-qualified closure; local Release starts successfully**.

Google Play Console live read: production remains 3.1.0 (57), fully rolled out; internal testing exposes 3.1.0 (65), fully available to internal testers. No unpublished changes were shown. No publication or configuration changes were made.

The Motorola base install was local debug 3.1.0 (54), installer null, certificate SHA-256 `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`. Play's application certificate is `6a8cb2e2cdd2c1fdd7c5cfcf00d7c3cc5861c2cd3faf49f69a312535a44fee0f` (Console key management). Direct Play replacement is incompatible with preserving this installed app's data. No uninstall or clearing was attempted.

Source already uses runtimeVersion fingerprint. The recorded historical diagnosis links v57's cold crash to incompatible Worklets/OTA JavaScript under a static runtime version. Today's checks do not newly reproduce v57 and do not prove the current Play v65 behavior.

Built the current source plus bounded chat/Explorer fixes using the canonical local production-apk Release script, with isolated lockfile dependencies and arm64-v8a. Both builds succeeded. Installed by data-preserving update after identity and certificate checks. The resulting app is non-debuggable but signed with the existing debug key; installer remains null.

Local cold-start proof: synthetic chat opened in 953 ms on the first build and all six final cold links passed on the second build (Explorer x3, guides, dictionary, Capture). These Android Activity times are not full-interaction readiness timings. Final APK SHA-256 `1caa91650a8507755e64bcf0a36a9f30991544c761fd9d1212f5bb65649d6361`.

Play Integrity remains unqualified: it requires an eligible Play installation reaching the backend. A second suitable device/profile or an explicitly authorized data-preserving migration/reinstallation plan is needed. No Hermes/Worklets disabling, EAS build, OTA rollback, Store upload, or production rollout was performed.
