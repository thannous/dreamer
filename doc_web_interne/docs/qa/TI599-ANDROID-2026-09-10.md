# TI-599 — Release deep-link qualification, 10 September 2026

Status: **in progress; authentication resume and process-cold notification not yet qualified**.

## Inventory and native baseline

Actual notification families: morning capture and optional streak/inactivity -> `/recording`; analysis ready -> `/journal/{id}`; weekly recap -> `/weekly-recap`. Native resource navigation uses `/explore`, `/symbol-dictionary`, `/dream-guides`, `/ritual/{id}`, `/sleep-sounds`. No new public sharing channel was added.

Base app `com.tanuki75.noctalia`, Motorola, local Release 3.1.0 (54), installer null, non-debuggable, matching existing debug signing certificate. Update installed with `adb install -r`, without uninstall or data clearing. Initial Release APK SHA-256 4b1faf900978b50749a06129ba65b8c6d4dcc6038b7ee1d39c26acf11e727605.

Fifteen direct-URI cases across foreground, background, force-stop: Capture, Explorer, weekly recap, own synthetic journal entry, nonexistent journal entry. Fourteen passed. Cold Explorer opened Capture. A nonexistent dream displayed the not-found state without leaking a different dream. Capture retained the existing 905-character draft in all three modes.

## Correction and final Release

The cold-launch allowlist omitted `explore`. Added this existing destination and three behavior cases for custom scheme, trailing slash, and trusted HTTPS mapping. All 33 onboarding/routing tests pass; focused lint passes.

Final local Release APK SHA-256 `1caa91650a8507755e64bcf0a36a9f30991544c761fd9d1212f5bb65649d6361`. Three repeated cold Explorer launches pass, as do cold guides, dictionary, and Capture. See `final-cold.json` for launch measurements and UI destination checks. The draft remains 905 characters.

## Actual notification test

Temporarily enabled the daily reminder and Android notification permission using the app's UI. A real scheduled test notification appeared in Android's shade. Tapping it from background opened Capture, preserved the 905-character draft, and Android Back exited without an observed navigation loop.

Second attempt used `am kill` while backgrounded; Android retained the process. Tapping still opened Capture, but this is **not process-cold evidence**. The daily reminder was restored off and Android notification permission restored denied, matching the initial user-facing state.

No new backend or Store publication. Play distribution and Play Integrity are separate TI-506 evidence. Owner-to-guest isolation passed (synthetic private chat unavailable after logout); logging back into the same owner restored chat access. No second authenticated account was used. The inaccessible chat showed not-found rather than a sign-in continuation, and automatic resumption of that destination after authentication is not qualified.
