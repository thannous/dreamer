# Astra instruction cleanup — 2026-09-12

Applied the eight recommendations from the read-only audit after the user's explicit “do it”. Basis: Eric Provencher's [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra), read on 2026-09-12.

## Applied changes

| Finding | Result |
| --- | --- |
| A01 — complete deliverables | `full-output-enforcement` no longer requires manual continuation or treats every task as production-critical. Complete files and scoped verification remain required. |
| A02 — discovery | Shortened SEO/social and revised design descriptions. Made four custom-model wrappers, the legacy design variant, the legacy skill-authoring alias, and the duplicate personal browser stub explicit-only using `agents/openai.yaml`. |
| A03 — design criteria | Removed simulated Python output, compulsory GSAP and static-interface bans from `gpt-taste`. Replaced the universal design preflight with applicable checks. |
| A04 — reference scope | `image-to-code` starts from supplied/selected references. Image counts follow requested coverage. Detailed design guidance moved into contextual references. |
| A05 — implementation authority | Root `AGENTS.md` distinguishes direct implementation requests from proposal/audit requests and preserves narrower authorization and publication boundaries. |
| A06 — proportional preparation | Root reading requirements depend on the change. Root and `animate-expo` distinguish local progress from release-device qualification. |
| A07 — Higgsfield catalog | Preset discovery/validation requires the live catalog; custom-style preparation does not. Updated the skill and its Phase 0 row, and replaced the long root exception with contextual links. |
| A08 — skill authoring | Replaced stale Cursor-specific authoring guidance with an explicit-only alias to the installed canonical `skill-creator`. |

Four refactored entrypoints:

| Personal skill | Before lines | After lines | Supporting references |
| --- | ---: | ---: | ---: |
| design-taste-frontend | 1,206 | 33 | 4 |
| image-to-code | 1,228 | 25 | 2 |
| imagegen-frontend-web | 987 | 24 | 3 |
| imagegen-frontend-mobile | 1,465 | 29 | 4 |

These are entrypoint reductions, not measured token, latency or quality improvements. Useful motion examples, visual extraction guidance, platform details, composition options and source links remain available on demand.

## Scope and preservation

- Personal changes: 10 skill bodies, 14 supporting references and 7 invocation-policy files, under `/Users/tanuki/.agents/skills` and `/Users/tanuki/.codex/skills`.
- Repository changes: `AGENTS.md`, local `.agents/skills/higgsfield-video-explainer/SKILL.md`, and this record. The local project skill is ignored by Git; it was verified separately.
- Custom wrappers are generated by `codex-router`. Their bodies and ownership markers were preserved; only separate supported invocation metadata was added. Their long source descriptions remain unchanged because the runtime owns those bodies.
- The explicit-only set is `codex-in-app-browser`, `codex-computer-use`, `codex-app-threads`, `codex-router`, `design-taste-frontend-v1`, `create-skill`, and the personal `agent-browser` stub. Explicit invocation remains available.
- No plugin caches, model/reasoning settings, app code, device data, remote systems or publication state changed. Pre-existing `.mcp.json` changes and untracked work were preserved.
- Before snapshots, staged content, exact file hashes, repository diffs, and validation results are at `/private/tmp/astra-instructions-20260912-lnx47c63`. This is a temporary backup location, not a permanent archive.

## Validation

- Canonical `skill-creator/scripts/quick_validate.py`: all 10 changed personal entrypoints passed, both staged and after saving.
- YAML parsing: all 7 invocation policies parsed as `allow_implicit_invocation: false`.
- Local reference checks: 29 links passed across affected skill bodies/references and repository instructions.
- All 31 personal output files matched their prepared SHA-256 hashes after saving. Six snapshotted but unchanged skill bodies also matched their original hashes, including generated wrappers.
- `git diff --check` passed. No app builds or suites were run for this instruction-only change.
- Python lacked PyYAML initially. Validation used an isolated temporary `uv` environment with PyYAML 6.0.3; no project dependencies were changed.

The following was a static scenario review of the resulting text, not an independent model execution or live UI test:

| Scenario | Boundary retained in the updated instructions |
| --- | --- |
| Complete several files | Finish requested files; no skill-imposed manual “continue”. |
| Audit a proposal | Report findings; do not infer implementation or publication authority. |
| Fix something locally | Implement and verify within the local-only restriction. |
| Build a calm static landing page | Static presentation is valid; no mandatory GSAP, extra theme or image quota. |
| Implement a supplied screenshot | Inspect the original and implement; generating another image cannot recover its exact text. |
| Generate one onboarding screen | Deliver the requested screen without inventing a flow. |
| Prepare a custom-style explainer while presets are unavailable | Continue independent preparation; verify the selected operation before submission. |
| Discover a named preset | Validate the identifier against the live catalog. |
| Motion work without release-device access | Continue authorized local work; leave motion feel unqualified. |
| Merge triggers production without publication intent | Publication boundary remains required. |

The running task's previously injected skill catalog was not refreshed. Fresh-task routing, real generated outputs, and comparative model performance remain unmeasured. No commit, push, merge or deployment was performed.
