import React from 'react';
import { LucidInfoPage } from '@/components/lucid/LucidInfoPage';
import { useLucidTrainer } from '@/context/LucidTrainerContext';
import { LUCID_EVIDENCE_REFERENCES } from '@/lib/lucid/content';

const LABEL = { en: { eyebrow: 'Evidence', limits: 'What this app does not claim', wellbeing: 'Wellbeing boundary', sources: 'Sources' }, fr: { eyebrow: 'Sources', limits: 'Ce que l’application ne promet pas', wellbeing: 'Limite bien-être', sources: 'Sources' }, es: { eyebrow: 'Evidencia', limits: 'Lo que la app no promete', wellbeing: 'Límite de bienestar', sources: 'Fuentes' }, de: { eyebrow: 'Evidenz', limits: 'Was die App nicht verspricht', wellbeing: 'Wellbeing-Grenze', sources: 'Quellen' }, it: { eyebrow: 'Prove', limits: 'Cosa l’app non promette', wellbeing: 'Limite di benessere', sources: 'Fonti' } } as const;
export default function LucidScienceScreen() { const { content } = useLucidTrainer(); const copy = LABEL[content.locale]; return <LucidInfoPage eyebrow={copy.eyebrow} title={content.science.title} subtitle={content.science.definition} icon="flask" sections={[{ title: content.science.evidenceSummary, body: content.science.uncertainty }, { title: copy.limits, bullets: content.science.boundaries }, { title: copy.wellbeing, body: `${content.science.sleepPriority}\n\n${content.science.supportAdvice}` }, ...LUCID_EVIDENCE_REFERENCES.map((reference) => ({ title: `${reference.authors} (${reference.year})`, body: `${reference.title}. ${reference.note}`, link: { label: `${copy.sources}: ${reference.identifier.type.toUpperCase()} ${reference.identifier.value}`, url: reference.url } }))]} />; }
